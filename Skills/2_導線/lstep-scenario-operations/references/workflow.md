# Lステップ シナリオ配信 exact メモ

## current 入口

- `シナリオ配信`
- 一覧 URL
  - `/line/content/group`
- 主要 API
  - `GET /api/content-group-groups`
  - `GET /api/content-groups?group={group_id}&page=1`
  - `GET /api/v1/content_groups/{id}`

## 一覧で見える主要ラベル

- `新しいフォルダ`
- `新しいシナリオ`
- `並び替え`
- `検索`
- row controls
  - `編集`
  - `挿入`
  - `コピー`
  - `プレビュー`
  - `別窓`
  - `テスト`
  - `削除`

## edit 画面で見る主要ラベル

- `シナリオ名`
- `シナリオフォルダ`
- `状態`
- `対象の絞り込み`
- `コンテンツ`
- `最終コンテンツ配信後の処理`

current edit URL pattern
- `/line/content/{content_group_id}?group={group_id}`

## step modal の主要ラベル

- `配信メッセージ日時編集`
- `配信タイミング`
  - `購読開始直後`
  - `経過時間指定`
- `step`
- `time`
- `order`
- `開始当日`
- `pause`
  - `シナリオを一時停止しない`
  - `シナリオを一時停止する`
- `送信先を絞り込む`
- `tag_limit`
- `tag_id`
- `テンプレート詳細`
- `テンプレート名`
- `テンプレート種別`
- `テンプレートフォルダ`
- `テンプレートを編集する`
- `テンプレートプレビュー`
- `アクションを設定しない`
- `アクションを設定する`
- `アクションを設定`
- `設定解除`

## current の読み方

- `シナリオ配信` は、message を並べる機能ではなく `順番と条件を持った行動設計` として読む
- step ごとに先に切るべき役割は次の 3 つ
  - `認識変換`
  - `行動誘導`
  - `状態更新`
- current の良いシナリオは、step ごとの役割が明確
- current の悪いシナリオは、1 step に複数目的を詰め込んでいる

## step での exact 判断

### テンプレートを使う時

- `テンプレート名`
- `テンプレート種別`
- `テンプレートフォルダ`
- `テンプレートプレビュー`
を先に見る。

### アクションを使う時

- reusable にしたい複合処理なら `アクション管理`
- その step に閉じた軽い処理なら inline

## good / bad

### good

- シナリオ全体の目的が 1 文で言える
- 各 step の役割が分かれている
- step の `テンプレート` と `アクション` の理由が説明できる
- `最終コンテンツ配信後の処理` まで意味が通っている

### bad

- 何の状態の人に始まるか分からない
- step が増えているだけ
- `テンプレート` と `アクション` を何となく付けている
- current / legacy を見ずに old シナリオを流用する

## exact review 順

1. 一覧で対象シナリオを見つける
2. row の `プレビュー` か `別窓` で全体像を見る
3. `編集` で step 構造を見る
4. 各 step の役割を `認識変換 / 行動誘導 / 状態更新` で置く
5. main CTA を持つ step を特定する
6. `テスト` で実機確認する
7. テスト用なら `削除`

## Addness 側で見るべき補足

- どの account か
- current / legacy
- 命名規則
- representative の意図

これらは `Master/knowledge/lstep_structure.md` を正本にする。
