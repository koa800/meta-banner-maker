# CDP（顧客マスターデータ）

最終更新: 2026-03-05（列名統一・データ補完・流入経路正規化）
ステータス: **初回同期完了・運用中**

---

## 概要・目的

アドネスの顧客データが複数のGoogleスプレッドシートに散在している状態を解消し、**1つのマスターシートに統合**する。

**目的:**
- 導線分析（どの広告→どのセミナー→どの商品で購入したか）
- セグメント別ナーチャリング（流入経路・ファネル進捗・LTVでグルーピング）
- 顧客解像度の向上（定性情報の一元管理）

**CDPスプレッドシート:** [CDP](https://docs.google.com/spreadsheets/d/1qjU279OVD0i4h2AdQzkYIsZCfA1BeiUKLHNg7i2a2fk/edit)

---

## タブ構成

| タブ | 用途 |
|------|------|
| **顧客マスタ** | メインテーブル（行1:グループ名、行2:カラム名、行3以降:データ） |
| **データソース管理** | カラムマッピング（CDPカラム→参照先URL/タブ/列）+ 運用ステータス |
| **マスター追加ルール** | 登録・名寄せ・更新のルール一覧 |
| **定義** | 各カラムの定義・データソース（4列: カラム名/グループ/定義/データソース） |
| **除外リスト** | スタッフ・テストデータの除外対象（集計から除外） |

---

## カラム設計（全58列）

### システム管理（3列）
顧客ID / 作成日 / 最終更新日（※同期・インポート時に自動更新）

### ID情報（3列）
メールアドレス（名寄せ第1キー） / 電話番号（名寄せ第2キー） / LINE名（参照用）

### 基本情報（13列）
姓 / 名 / フリガナ（姓） / フリガナ（名） / 性別 / 生年月日 / 年齢（※1） / 都道府県 / 市区町村 / 住所1 / 住所2 / 職業 / 年収

**※1 年齢の決定ルール:**
1. 生年月日がある → 今日時点の正確な年齢を自動計算（例: `35`）
2. 生年月日がない → アンケート回答の年代を使用（例: `30代`）

### 定性情報（3列）
現在の悩み / 目指す目標 / スキル経験

### 流入情報（7列）
初回流入日 / 初回流入経路 / 最新流入日 / 最新流入経路 / 総流入経路数 / CR名 / CRリンク

### ファネル進捗（7列）
セミナー予約日（カンマ区切り） / セミナー予約合計 / 個別予約日（カンマ区切り） / 個別予約合計 / 個別着座日（カンマ区切り） / 担当営業 / 収録URL

### 商品購入（9列）
初回購入日 / 初回購入商品 / 最新購入日 / 最新購入商品 / 累計購入回数 / 購入商品 / 着金売上 / 返金額 / LTV

### スキルプラス利用状況（13列）
入会日 / プラン / サポートURL / 講義保管庫ID / 連続アクション日数 / アクション最終日 / 累計アクション日数 / ゴール達成数 / 初回イベント参加日 / 最終イベント参加日 / イベント参加回数 / クーリングオフ日 / 中途解約日

---

## データソース管理

CDPカラム単位のカラムマッピング形式。各カラムの参照先と運用状況を1枚で管理する。

**列構成（14列）:**

| 列 | 内容 |
|----|------|
| CDPカラム | 外部ソースから同期するカラム名（自動生成/自動計算/手動は含まない） |
| グループ | カラムのグループ名 |
| ソース元 | データソース（プルダウン選択） |
| 優先度 | 同一カラムに複数ソースがある場合の優先順位（1が最優先） |
| スプレッドシートURL | 参照先のURL |
| タブ名 | 参照先タブ |
| 参照先列 | 参照先の列名 |
| 正規化 | 取り込み時の変換ルール |
| 入力条件 | 参照データを顧客マスタに入力するための条件 |
| ステータス | 正常/更新なし/停止（自動更新） |
| 最終同期日 | 同期スクリプトが自動記録 |
| 更新数 | 同期時の更新件数 |
| エラー数 | 同期時のエラー件数 |
| メモ | 備考 |

**複数ソース対応:** 同じCDPカラムに複数行を追加し、優先度で順序を指定。同期時は優先度1から順にチェックし、最初に値が見つかったものを採用する。

**同期対象外のカラム（自動計算 or システム管理のため、データソース管理に含まない）:**
顧客ID / 作成日 / 最終更新日 / 総流入経路数 / セミナー予約合計 / 個別予約合計 / 累計購入回数 / 購入商品 / LTV

**ステータス判定ルール:**
- 正常: 同期が正常終了
- 更新なし: 最終同期日から7日以上経過
- 停止: 同期がエラーで失敗

**ソース元（プルダウン選択肢）:**
Lステップ / UTAGE / Mailchimp / Univapay / MOSH / Invoy / 決済システム / 銀行振込 / 講義保管庫 / スプレッドシート

---

## 登録・名寄せルール

- **登録条件**: メールアドレスまたは電話番号が必須（LINE名のみでは登録しない）。さらにイベント発生（アンケート回答・セミナー予約・購入等）が必要。LINE登録のみ・メール登録のみの人は集客データシートに格納
- **名寄せ（自動統合）**: メールアドレス一致 or 電話番号一致 → 統合
- **LINE名のみ**: 自動統合しない（重複リスクが高い）。後からメール/電話番号が判明したら統合
- **重複排除**: 複数ソースで同じメールアドレスがあれば既存レコードに統合（新規追加しない）
- **除外リスト**: スタッフ・テストデータは除外リストタブに登録（メール・電話番号のいずれかに一致したら除外）し、マスタに追加しない。LINE名は重複が多いため除外キーに使わない
- **スパム自動検知**: 除外リストに加え、同期時にパターンマッチでテスト/いたずらメールを自動除外（`is_spam_email()`）
  - test系: `test@`, `test123@` 等
  - 同一文字繰り返し: `aaaa@gmail.com` 等（フリーメールのみ）
  - テストドメイン: `example.com`, `test.com` 等
- **更新方式**: 名寄せで既存行を更新。新規顧客のみ最下部に追加
- **複数ソース**: 同一カラムに複数ソースがある場合、優先度順に値を探し最初のヒットを採用

### 同期の競合ルール

| 状況 | ルール |
|------|--------|
| 新規顧客 | 優先度1のソースから値を取得。なければ優先度2を確認 |
| 既存顧客（値が空） | 優先度順にソースを確認し、最初に見つかった値を記録 |
| 既存顧客（値が入っている） | **最終更新日が新しい方を採用**。ソース側の方が新しければ上書きする |
| 値が矛盾する場合 | 最終更新日が新しいソースの値を優先。変更前後の値をログに記録 |

---

## データ正規化ルール

| データ種別 | 統一形式 | 例 |
|-----------|---------|-----|
| 電話番号 | ハイフンなし、+81→0変換 | `09012345678` |
| メールアドレス | そのまま保存（照合時のみ大文字小文字を無視） | |
| 金額 | ¥ + 3桁カンマ区切り | `¥1,200,000` |
| 日付 | YYYY/MM/DD | `2026/03/05` |
| 郵便番号 | ハイフンあり（3桁-4桁） | `150-0001` |
| 姓名 | 姓と名を分割して保存（姓辞書ベースの自動分割） | |

---

## データ保護

### 電話番号の保護
- 顧客マスタの電話番号列は **Plain Text形式** に設定済み
- Googleスプレッドシートが数値に自動変換して桁が壊れるのを防止
- 同期時にバリデーション実行（科学的記数法検出・桁数チェック）

### メールアドレスの重複検知
- 新規メール登録時にレーベンシュタイン距離で類似メールを検出（閾値: 距離2以下）
- 同一ドメインのメールのみ比較（パフォーマンス最適化）
- 警告ログを `System/data/cdp_email_warnings.json` に記録

### 入力バリデーション（同期・インポート時に自動適用）
- **メールアドレス**: RFC準拠の形式チェック（`is_valid_email()`）
- **電話番号**: 10〜11桁チェック・科学的記数法検出（`validate_phone()`）
- **年齢**: 数字1〜3桁 + optional 代/歳 のみ許可（`is_valid_age()`）
- **URL系（収録URL / サポートURL / CRリンク）**: http/https で始まる形式のみ許可（`is_valid_url()`）

### 変更ログ
- 同期時に全変更の before/after を `System/data/cdp_change_log.json` に記録
- タイムスタンプ・行キー・カラム・旧値・新値・ソースを保持
- 最大10,000件を保持（古い方から自動削除）

---

## アドネスの決済システム

| 決済方法 | システム | 備考 |
|---------|---------|------|
| クレジットカード | Univapay | メイン決済 |
| クレジットカード | MOSH | 一部商品で併用 |
| クレジットカード | Invoy | |
| 銀行振込 | 銀行振込 | B2B・一部バックエンド |

※ 商品マスタ（`1kxUbLqhnzLC1Pg0ASVgU135bnx4Rsv_jP0pqGC0R69w`）で最新状況を確認

---

## 容量試算・スケール戦略

- **現在**: 58列 x 45,873人 = 266万セル（Google Sheets上限1,000万セルの27%）
- **対応可能**: 〜17万人程度まではシート分割不要
- **将来**: 17万人超 → アクティブ/アーカイブでシート分割
- **行数不足時**: 同期スクリプトが自動拡張（空き行100未満で+500行追加）

### 集客データ（リードプール）

CDPマスタとは別シートで管理。メールアドレスで登録したリード（イベント未発生者）を格納する。

- **集客データシート**: [集客データ](https://docs.google.com/spreadsheets/d/1iD3DGxNhZruyjYcA5n6oXRDk2ZGA3uMDOo0stQS9Y00/edit)
- CDPマスタには含めない（容量上限を超えるため）
- ファネル分析時はスクリプトでCDPマスタとメールアドレスで突合

**タブ構成: メール集客データ（1タブのみ・3列）**

| カラム | 説明 |
|--------|------|
| 登録日 | フォーム回答日・オプトイン日 |
| メールアドレス | CDPマスタとの突合キー |
| 初回流入経路 | 広告/LP名/オーガニック等。昇格時にCDPマスタの初回流入経路に引き継ぐ |

**容量試算:** 30万行×3列 = 90万セル（上限1000万の9%。余裕あり）

**マスタ追加時の掃除:**
- cdp_sync.pyがCDPマスタに新規追加した際、そのメールアドレスが集客データシートにあれば行を削除する
- 初回流入経路は集客データからCDPマスタに引き継ぐ

**LINE集客データタブを作らない理由（2026-03-05 意思決定）:**
1. 1人が複数の公式LINEに登録（10アカウント等）するため、LINE名での名寄せ・正確なリード数把握が不可能
2. LINE登録のみの人にはLステップ上でシナリオ配信しており、アプローチはLステップ側で完結する
3. シナリオの成果（開封率・クリック率・セミナー予約率）もLステップ管理画面で確認可能
4. メールアドレスがない人はCDPマスタと突合できず、集客データシートの役割（転換率分析）に乗らない
→ LINE登録者の管理・アプローチ・計測はすべてLステップで完結。シートに持つ必要なし

---

## 同期の仕組み

### 自動同期フロー

```
データソース管理タブ（マッピング）
  → cdp_sync.py がマッピングを読み取り
    → ソースごとにシートを読み込み
      → 除外チェック → 名寄せ → バリデーション → 書き込み
        → ステータス・同期日・更新数をデータソース管理に記録
```

### 排他制御

- 同期開始時にロックファイル（`System/data/cdp_sync.lock`）を取得
- ロック中に別の同期が実行されるとブロック（「同期がロック中です」エラー）
- 10分経過でロック自動解除（デッドロック防止）
- 同期完了 or エラー時にロック自動解除

### 増分同期

- ソースごとの最終同期状態を `System/data/cdp_sync_state.json` で追跡
- 前回同期時の行数を記録し、差分（新しい行）のみ処理
- `--full` オプションで全件同期も可能

### ソース変更検知

同期前にソースの構造変化を自動検出し、知らない間に違うデータを取り込むことを防ぐ。

| 検知パターン | 動作 |
|-------------|------|
| マッピング対象の列名が消失 | **同期を中断**（データ欠損を防止） |
| 列の追加・削除（マッピング外） | アラート表示して続行 |
| 行数が50%以上急減（100行以上のソース） | アラート表示して続行 |

- ヘッダーのスナップショットを `cdp_sync_state.json` に保存し、次回同期時に比較
- 初回同期時はスナップショットがないため検知なし（2回目以降から有効）

### バッチ書き込み

- 既存行の更新: `batch_update()` で500件ずつチャンク分割
- 新規行の追加: `update()` で500行ずつ範囲指定書き込み（`append_rows()`はグリッド膨張リスクあり不使用）
- グリッドが足りない場合は自動拡張
- カンマ区切りカラム（セミナー予約日、個別予約日、個別着座日）は上書きではなく追記（重複チェック付き）

### CLIコマンド

```bash
python3 cdp_sync.py sync              # 全ソース自動同期（増分）
python3 cdp_sync.py sync --dry-run    # ドライラン
python3 cdp_sync.py sync --full       # 全件同期
python3 cdp_sync.py check-phones      # 電話番号バリデーション
python3 cdp_sync.py status            # ステータス確認
python3 cdp_sync.py exclusion-list    # 除外リスト表示
```

---

## 今後の実装予定

- [x] CDPスプレッドシートの設計・構築
- [x] カラムマッピング（データソース管理）設計
- [x] 除外リスト機能
- [x] 同期スクリプト基盤（cdp_sync.py）
- [x] 電話番号の保護（Plain Text形式 + バリデーション）
- [x] メールアドレスの重複検知（Levenshtein距離）
- [x] 変更ログ記録（before/after）
- [x] 増分同期対応
- [x] 自動同期の本実装（マッピング読み取り→顧客マスタ更新）
- [x] データソースの登録（個別予約データ・面談記入_DB）
- [x] 初回同期（31,831件の顧客データ取り込み完了）
- [x] 決済履歴CSVインポート（8,981件更新 + 12,339件新規追加）
- [x] 累計購入回数の自動計算（ARRAYFORMULA）
- [x] 姓名分割ロジックの実装（既存データから姓辞書構築→自動分割。12,505件分割済み）
- [x] スパム/テストメールの自動検知（test系・同一文字繰り返し・テストドメイン）
- [x] ARRAYFORMULA列の書き込み保護（セミナー予約合計・個別予約合計・累計購入回数）
- [x] フィルタ機能の設定（全カラムで並び替え・絞り込み可能）
- [x] ソース変更検知（ヘッダー変更→中断、行数急減→アラート）
- [ ] LINE登録のみの人をマスタから集客データシートに移行（アンケート未回答・イベント未発生の人を抽出→移行→マスタから削除）
- [ ] Orchestrator連携（日次で自動同期）
- [ ] 導線分析レポートの自動生成

---

## ファイル構成

| ファイル | 説明 |
|---------|------|
| `Project/4_AI基盤/CDP.md` | 本ドキュメント |
| `System/cdp_sync.py` | 同期スクリプト（除外・名寄せ・バリデーション・変更ログ・増分同期） |
| `System/cdp_csv_import.py` | LステップCSVワンショットインポート |
| `System/cdp_payment_import.py` | 決済履歴CSVインポート（購入データ・姓名・LINE名） |
| `System/data/cdp_change_log.json` | 変更ログ（同期時のbefore/after記録） |
| `System/data/cdp_sync_state.json` | 増分同期の状態管理 |
| `System/data/cdp_email_warnings.json` | メール類似検知の警告ログ |
| `System/data/cdp_sync.lock` | 排他制御ロックファイル（同期中のみ存在） |
