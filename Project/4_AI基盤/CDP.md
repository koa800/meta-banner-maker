# CDP（顧客マスターデータ）

最終更新: 2026-03-05
ステータス: **設計完了・初期構築済み**

---

## 概要・目的

アドネスの顧客データが複数のGoogleスプレッドシートに散在している状態を解消し、**1つのマスターシートに統合**する。

**目的:**
- 導線分析（どの広告→どのセミナー→どの商品で購入したか）
- セグメント別ナーチャリング（流入経路・ファネル進捗・LTVでグルーピング）
- 顧客解像度の向上（定性情報の一元管理）

**CDPスプレッドシート:** [CDP](https://docs.google.com/spreadsheets/d/1qjU279OVD0i4h2AdQzkYIsZCfA1BeiUKLHNg7i2a2fk/edit)

---

## タブ構成

| タブ | 用途 |
|------|------|
| **顧客マスタ** | メインテーブル（行1:グループ名、行2:カラム名、行3以降:データ） |
| **データソース管理** | カラムマッピング（CDPカラム→参照先URL/タブ/列）+ 運用ステータス |
| **マスター追加ルール** | 登録・名寄せ・更新のルール一覧 |
| **定義** | 各カラムの定義・データソース（4列: カラム名/グループ/定義/データソース） |
| **除外リスト** | スタッフ・テストデータの除外対象（集計から除外） |

---

## カラム設計（全60列）

### システム管理（3列）
顧客ID / 作成日 / 最終更新日

### ID情報（4列）
メールアドレス（名寄せ第1キー） / 電話番号（名寄せ第2キー） / LINE名（参照用） / 講義保管庫ID

### 基本情報（15列）
姓 / 名 / フリガナ（姓） / フリガナ（名） / 性別 / 生年月日 / 年齢 / 国 / 都道府県 / 市区町村 / 郵便番号 / 住所1 / 住所2 / 職業 / 年収

### 定性情報（3列）
現在の悩み / 目指す目標 / スキル経験

### 流入情報（7列）
新規流入日 / 新規流入経路 / 最新流入日 / 最新流入経路 / 総流入経路数 / 広告CR名 / 広告CR動画URL

### ファネル進捗（9列）
初回セミナー予約日 / 最終セミナー予約日 / セミナー予約合計 / 初回個別予約日 / 最終個別予約日 / 個別予約合計 / 初回個別着座日 / 最終個別着座日 / 録画URL

### 商品購入（9列）
初回購入日 / 初回購入商品 / 最終購入日 / 最終購入商品 / 累計購入回数 / 購入商品 / 着金売上 / 返金額 / LTV

### スキルプラス利用状況（10列）
入会日 / サポートURL / 連続アクション日数 / アクション最終日 / 累計アクション日数 / ゴール達成数 / 初回イベント参加日 / 最終イベント参加日 / イベント参加回数 / クーリングオフ日 / 中途解約日

---

## データソース管理

CDPカラム単位のカラムマッピング形式。各カラムの参照先と運用状況を1枚で管理する。

**列構成（13列）:**

| 列 | 内容 |
|----|------|
| CDPカラム | 顧客マスタのカラム名 |
| グループ | カラムのグループ名 |
| ソース元 | データソース（プルダウン選択） |
| 優先度 | 同一カラムに複数ソースがある場合の優先順位（1が最優先） |
| スプレッドシートURL | 参照先のURL |
| タブ名 | 参照先タブ |
| 参照先列 | 参照先の列名 |
| 正規化 | 取り込み時の変換ルール |
| ステータス | 正常/更新なし/停止（自動更新） |
| 最終同期日 | 同期スクリプトが自動記録 |
| 更新数 | 同期時の更新件数 |
| エラー数 | 同期時のエラー件数 |
| メモ | 備考 |

**複数ソース対応:** 同じCDPカラムに複数行を追加し、優先度で順序を指定。同期時は優先度1から順にチェックし、最初に値が見つかったものを採用する。

**ステータス判定ルール:**
- 正常: 同期が正常終了
- 更新なし: 最終同期日から7日以上経過
- 停止: 同期がエラーで失敗

**ソース元（プルダウン選択肢）:**
Lステップ / UTAGE / Mailchimp / Univapay / MOSH / 銀行振込 / 講義保管庫 / 自動生成 / 自動計算 / 手動

---

## 登録・名寄せルール

- **登録条件**: 全員登録（メール、LINE、電話番号、いずれか1つでもあれば）
- **名寄せ（自動統合）**: メールアドレス一致 or 電話番号一致 → 統合
- **LINE名のみ**: 自動統合しない（重複リスクが高い）。後からメール/電話番号が判明したら統合
- **重複排除**: 複数ソースで同じメールアドレスがあれば既存レコードに統合（新規追加しない）
- **除外リスト**: スタッフ・テストデータは除外リストタブに登録し、マスタに追加しない
- **更新方式**: 名寄せで既存行を更新（積み上げ方式ではない）。新規顧客のみ最下部に追加
- **複数ソース**: 同一カラムに複数ソースがある場合、優先度順に値を探し最初のヒットを採用

---

## データ正規化ルール

| データ種別 | 統一形式 | 例 |
|-----------|---------|-----|
| 電話番号 | ハイフンなし、+81→0変換 | `09012345678` |
| メールアドレス | そのまま保存（照合時のみ大文字小文字を無視） | |
| 金額 | ¥ + 3桁カンマ区切り | `¥1,200,000` |
| 日付 | YYYY/MM/DD | `2026/03/05` |
| 郵便番号 | ハイフンあり（3桁-4桁） | `150-0001` |
| 姓名 | 姓と名を分割して保存 | |

---

## アドネスの決済システム

| 決済方法 | システム | 備考 |
|---------|---------|------|
| クレジットカード | Univapay | メイン決済 |
| クレジットカード | MOSH | 一部商品で併用 |
| クレジットカード | Invoy | |
| 銀行振込 | 銀行振込 | B2B・一部バックエンド |
| その他 | CBS | |

※ 商品マスタ（`1kxUbLqhnzLC1Pg0ASVgU135bnx4Rsv_jP0pqGC0R69w`）で最新状況を確認

---

## 容量試算・スケール戦略

- **現在**: 60列 x 10万人 = 600万セル（Google Sheets上限1,000万セルの60%）
- **対応可能**: 〜16万人程度まではシート分割不要
- **将来**: 17万人超 → アクティブ/アーカイブでシート分割
- **行数不足時**: 同期スクリプトが自動拡張

---

## 今後の実装予定

- [x] CDPスプレッドシートの設計・構築
- [x] カラムマッピング（データソース管理）設計
- [x] 除外リスト機能
- [x] 同期スクリプト基盤（cdp_sync.py）
- [ ] データソースの登録（Lステップ等のURL・タブ・列を入力）
- [ ] 自動同期の本実装（マッピング読み取り→顧客マスタ更新）
- [ ] Orchestrator連携（日次で自動同期）
- [ ] 姓名分割ロジックの実装
- [ ] 導線分析レポートの自動生成

---

## ファイル構成

| ファイル | 説明 |
|---------|------|
| `Project/4_AI基盤/CDP.md` | 本ドキュメント |
| `System/cdp_sync.py` | 同期スクリプト（除外チェック・名寄せ・ドライラン） |
