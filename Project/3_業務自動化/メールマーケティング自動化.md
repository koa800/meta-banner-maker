# メールマーケティング自動化（CDP × Mailchimp × AI）

最終更新: 2026-03-15
ステータス: **設計完了・着手待ち**

---

## 概要・目的

CDPからセグメントを切り出し、AIがメール本文をライティングし、Mailchimp APIで配信設定・送信まで自動完結するフローを構築する。

**目的:**
- セグメント別のメールマーケティングを自動化
- AIライティング → 承認 → 送信のワークフロー確立
- 将来的に承認率が安定したら全自動化

---

## Mailchimp現状（2026-03-06調査）

### アカウント情報

| 項目 | 値 |
|------|------|
| アカウント | アドネス株式会社 |
| プラン | **Premium**（最上位） |
| サーバープレフィックス | **us5** |
| コンタクト数 | 347,788 / 300,000（上限超過・追加課金中） |
| メール登録者 | 282,618 |
| 送信残り | 3,832,964 / 4,500,000 |
| APIキー | 発行済み（7件）。最新: `mailchimp/Lステップ顧客管理ツール用`（2026-02-23） |

### ドメイン認証状況

| ドメイン | ステータス | 用途 |
|---------|----------|------|
| **addness.co.jp** | Authenticated | メイン（メルマガ配信に使用可能） |
| **service.addness.co.jp** | Authenticated | サービス通知用 |
| **snsers.com** | Authenticated | センサーズ用 |
| kimeragon.jp | Needs authentication | 未認証 |
| wagokoro.co.jp | Needs authentication | 未認証（和心 = 別事業） |
| zent-8.com | Needs authentication | 未認証 |

→ **addness.co.jp は認証済み。API経由の配信にそのまま使える。**

### 既存のタグ（283個）

命名パターン:
- `UTAGE_Snsers_FB-Optin` — UTAGE経由・センサーズ・Facebookオプトイン
- `UTAGE_AI_FB_Optin` — UTAGE経由・AI・Facebookオプトイン
- `afad_seminar_optin_e_meta` — セミナーオプトイン・Meta広告
- `freeplan_Buy_*` — フリープラン購入者（媒体別）
- `*_buy` — 購入タグ（商品 × 媒体）
- `skillplus_buy_*` — スキルプラス購入者
- `Meta_Lightplan_Optin` — ライトプランオプトイン

→ 既存タグ体系は「流入経路 × アクション」が基本。CDPセグメントタグは `cdp_` プレフィックスで分離する。

### 既存セグメント（37個）

- オプト計測用、フリープラン購入者、直売りセミナー過去個別未予約者、SNS/AI導線別リスト等
- 「Last ran: 18 hours ago」のものもあり、現在も運用中

### 直近のキャンペーン実績

| キャンペーン | 配信数 | 開封率 | クリック率 |
|------------|--------|--------|----------|
| 2/28 AIセミナーPR_2通目 | 256,776 | 3.9% | 0.1% |
| 2/28 AIセミナーPR_2通目（フリープラン） | 6,493 | 26.8% | 1.3% |
| 2/28 AIセミナーPR_1通目（フリープラン） | — | 27.2% | 1.9% |

→ 全体配信は開封率3.9%だが、セグメント配信は**26-27%**。セグメント活用の効果が明確。

---

## live 確認済みの範囲

- login と `2段階認証` の current 導線
- regular `Campaign` の draft 作成 / 削除
- `Customer Journeys -> Build from scratch -> Name flow -> Audience -> trigger`
- `report` の `Recipients -> Open rate -> Click rate -> click-details -> downstream`
- `saved segment 1件` の create / delete
  - helper: `python3 System/scripts/mailchimp_segment_helper.py`
  - 2026-03-16 は `zz_test_segment_20260316_exact`
  - `id=4641613` を create し、delete 後 `204` まで確認
- `tag-search` の一覧読解
  - helper: `python3 System/scripts/mailchimp_tag_helper.py list-tags --limit 10`
- `search-members` による safe exploratory member 探索
  - helper: `python3 System/scripts/mailchimp_tag_helper.py search-members --query "<query>" --limit 10`
- `tag 1件` の add -> rollback
  - 2026-03-16 は `koa800sea.nifs+1006@gmail.com`
  - `status=pending`
  - `tags_count=0`
  - `zz_test_tag_20260316_exact` を `add-tag -> member確認(tags_count=1) -> remove-tag -> member確認(tags_count=0)` まで live exact 済み
- current matrix の snapshot
  - helper: `python3 System/scripts/mailchimp_journey_snapshot.py --list-current --count 20`
- `Journey 1本` の snapshot
  - helper: `python3 System/scripts/mailchimp_journey_snapshot.py --query \"<journey_name_substring>\"`

## まだ live create / rollback を厚くすべき範囲

- `Journey 1本` の `trigger -> filter -> email step -> Send Test Emails -> cleanup`
- `tag 1件` の UI 側 create -> rollback
  - safe test member が分かれば `python3 System/scripts/mailchimp_tag_helper.py add-tag --email <safe_test_member> --tag zz_test_xxx`
  - rollback は `python3 System/scripts/mailchimp_tag_helper.py remove-tag --email <safe_test_member> --tag zz_test_xxx`
- actual case を使った漏斗診断の本数追加

### current browser blocker の exact 状態

- `python3 System/scripts/mailchimp_journey_create_delete_probe.py`
  - 先に `ensure_login(AUTOMATIONS_URL)` を実行するように修正済み
  - それでも current は browser が `https://login.mailchimp.com/?referrer=%2Fcustomer-journey%2F` に落ち、`LOGIN_NEEDS_TFA` で止まる回がある
- つまり current の残差は
  - API 読解ではなく
  - `Customer Journeys` の browser exact 化を、2段階認証を通した session で安定させること

## 技術的実現可能性: 結論

**全て実現可能。** Mailchimp Marketing APIで以下が全てプログラマブルに操作できる:

| やりたいこと | API | 制約 |
|-------------|-----|------|
| CDPコンタクトの一括同期 | `POST /lists/{id}/members` (バッチ対応) | 500件/リクエスト、同時接続10 |
| タグの付与・削除 | `POST /lists/{id}/members/{hash}/tags` | — |
| セグメント作成 | `POST /lists/{id}/segments` | タグ条件でフィルタ可能 |
| キャンペーン作成 | `POST /campaigns` | type: regular, recipients で対象指定 |
| メール本文の設定 | `PUT /campaigns/{id}/content` | HTML or プレーンテキスト |
| 配信スケジュール | `POST /campaigns/{id}/actions/schedule` | UTC指定 |
| 即時送信 | `POST /campaigns/{id}/actions/send` | 取消不可 |
| 配信結果の取得 | `GET /reports/campaigns/{id}` | 開封・クリック・バウンス等 |

**公式Pythonライブラリ:** `mailchimp-marketing`（pip install mailchimp-marketing）

**API制限:**
- 同時接続: 10
- バッチ: 500件/リクエスト（大量同期はバッチエンドポイント使用）
- レート: 429エラー時はリトライ

---

## 設計

### 全体フロー

```
CDP（Google Sheets: 56,538人）
  ↓ cdp_mailchimp.py sync
Mailchimpオーディエンス同期（メール・名前・タグ）
  ↓ cdp_mailchimp.py segment "セミナー予約 & 未購入"
CDPでフィルタ → 該当者に cdp_ タグ付与
  ↓ cdp_mailchimp.py draft --segment cdp_seminar_no_purchase --subject "..."
AIがメール本文をライティング → Mailchimpキャンペーン下書き作成
  ↓ Slack/LINEで承認依頼
甲原さんが確認 → 承認
  ↓ cdp_mailchimp.py send {campaign_id}
Mailchimp APIで送信
  ↓ 配信後
結果レポートを自動取得 → Slack通知
```

### セグメント設計

CDPの全58列を使って、Mailchimp単体では不可能な高精度セグメントが切れる:

| セグメント例 | CDP条件 |
|-------------|---------|
| セミナー参加 → 未購入 | セミナー予約日 あり & 累計購入回数 = 0 |
| LTV上位（リピーター） | LTV > ¥500,000 |
| Meta広告経由 × フリープラン未購入 | 初回流入経路 contains "Meta" & 購入商品 not contains "フリープラン" |
| 休眠顧客（最終購入90日以上） | 最新購入日 < 90日前 |
| スキルプラス会員 × アクション停止 | 入会日 あり & アクション最終日 < 30日前 |

### タグ命名規則

``` 
cdp_{セグメント名}
例: cdp_seminar_no_purchase, cdp_ltv_high, cdp_dormant_90d
``` 

既存タグ（UTAGE_*, freeplan_* 等）とは `cdp_` プレフィックスで明確に分離。

補足:
- Mailchimp は海外ツールなので、新規の tag や segment は日本語ではなく英語で作る
- Addness の current でも `media_funnel_event` 系の英語寄せを基本にする

### Journey と Campaign の使い分け

- `Journey`
  - 今後も同じシナリオを流すと決まっている時に使う
  - 役割は evergreen 用の自動化
  - 例: オプトイン後教育、個別3日間、購入後一定シナリオ
- `Campaign`
  - 1回きりのメール配信で、きちんと segment を切って送る時に使う
  - 役割は promotion 用の単発配信
  - 例: 期間限定プロモーション、特定条件の一斉案内
- current で確認できた representative campaign
  - `3/11 AIキャリアセミナーPR_2通目`
    - `Subscribed` 全体へ 255,250 通
  - `3/10 セミナーPR_3通目 (フリープラン)`
    - `freeplan_Buy` tagged contacts に 5,044 通
- つまり current の campaign は `今この条件に当てはまる人だけへ単発で打つ` 運用になっている

#### Journey state を汚さない current rule

- `Journey` の入口条件は `trigger` と `Filter who can enter` で閉じる
- `Campaign` の `saved segment` や一時的な配信条件を、そのまま `Journey` の入口へ持ち込まない
- `Journey` で新規 tag を作る時は、その tag が
  - 何の event か
  - downstream で何を起動するか
  を 1 文で言える時だけ作る
- `今回だけこの条件の人に送る` が主語に出た時点で、まず `Campaign` を疑う

### 入口選択の exact な問い

Mailchimp で迷いやすいのは、`メールを送りたい` から始めてしまうことです。最初に次を切る。

- 今やりたいのは `同じ顧客体験を繰り返し自動で流す` か
  - なら `Customer Journeys`
- 今やりたいのは `今この条件の人に 1 回だけ送る` か
  - なら `Campaigns`
- 今やりたいのは `誰に送るかの条件を作る` か
  - なら `tag / saved segment`
- 今やりたいのは `送った後の結果を読む` か
  - なら `report`

つまり Mailchimp では、最初の 1 手は `Journey か Campaign か` です。

- evergreen か
- 単発 promotion か
- audience 条件づくりか
- 結果の読解か

を先に切る。

#### 10秒判断

- `Journey`
  - 同じ顧客体験を evergreen で流す
  - tag で入口条件を切れる
- `Campaign`
  - 今回だけの promotion や告知
  - segment や exclude が主役
- `tag / saved segment`
  - 先に対象条件を作らないと本文に入れない
- `report`
  - 配信後の数値と downstream を読む

この 10 秒判断が言えない時は、`Build from scratch` や `Create` を押さない。

#### current Campaign の representative 例

- `3/11 AIキャリアセミナーPR_2通目`
  - type: `regular`
  - status: `sent`
  - emails_sent: `255,250`
  - segment: なし
  - 解釈:
    - 広い母集団へ打つ全体 promotion
    - evergreen ではなく、日付付きの単発告知
- `3/10 セミナーPR_3通目 (フリープラン)`
  - type: `regular`
  - status: `sent`
  - emails_sent: `5,044`
  - saved segment: `4640686`
  - 条件:
    - `StaticSegment static_is 4640686`
  - 解釈:
    - `フリープラン購入者` のような切られた母集団へ、1回だけ強く打つ campaign
- `3/11 AIキャリアセミナーPR_1通目 (copy 02)`
  - type: `regular`
  - status: `save`
  - emails_sent: `0`
  - segment: なし
  - 解釈:
    - まだ送っていない draft。current 企画候補ではあるが、実績ベースの current 配信とは分けて扱う
- `Resend: AIキャンプ (copy 01)`
  - type: `regular`
  - status: `save`
  - emails_sent: `0`
  - saved segment: `4641488`
  - 解釈:
    - segment は切ってあるが未送信。`copy` が付いていても、送っていなければ実績ベースの current 配信ではない

つまり Mailchimp は
- `同じ顧客体験を繰り返し自動で流す = Journey`
- `今この相手にだけ1回送る = Campaign`
で切るのが current の正しい理解

#### 実装前の最小チェック

- `Journey` か `Campaign` かを 1 文で説明できるか
- `誰に送るか` を `tag / saved segment / exclude` で言えるか
- 1 通の目的を 1 つに絞れているか
- main CTA の short.io を先に用意しているか
- 表示文字列ではなく hyperlink 実体を最後に click で確認する前提になっているか

#### Mailchimp の認証フロー

current の Addness では、Mailchimp ログイン時に 2 段階認証が入ることがある。  
実運用の exact 順は次です。

1. `Send code via SMS`
2. `Mailchimp認証` の LINE グループでコード確認
3. `verify` 画面へ入力

見た目だけ login 画面を抜けても、`verify` が残っている時は builder や report へ進まない。

#### 直近 regular campaign の CTA 型の実測

- 直近 12 本の regular campaign を content HTML で確認すると、sent 済みの current regular はほぼ `direct LIFF` が main CTA だった
- representative
  - `3/11 AIキャリアセミナーPR_2通目 = direct LIFF`
  - `3/10 セミナーPR_3通目 (フリープラン) = direct LIFF`
  - `2/28 AIセミナーPR_1通目 = direct LIFF`
  - `AI新卒プロモーション_2通目(2/21) = direct LIFF`
- 一方で draft 側には
  - `Resend: AIキャンプ (copy 01) = UTAGE`
  のように、CTA 型が変わり始めているものもあった
- さらに sent 側でも
  - `Resend: AIキャンプ = UTAGE`
  - `AIキャンプ = UTAGE`
  - `みかみの秘密合宿プロモ2通目 = UTAGE`
  のように、promotion の種類によっては UTAGE ページ主導のものもある
- 解釈
  - historical / current 実態の把握は `regular campaign = direct LIFF 多め`
  - ただし `regular campaign = 全て direct LIFF` ではなく、商品やオファーによって `UTAGE` 型も混ざる
  - 今後こちらが新規で作る標準は `main CTA = short.io`
  と分けて扱うのが安全

#### 直近 sent regular campaign の実測

- `AI全自動PR_1通目(3/13)`
  - `sent = 259,813`
  - `open_rate = 8.57%`
  - `click_rate = 0.187%`
  - main CTA は `direct LINE`
  - follow family は `%40631igmlz`
- `3/11 AIキャリアセミナーPR_2通目`
  - `sent = 255,250`
  - `open_rate = 10.59%`
  - `click_rate = 0.043%`
  - main CTA は `direct LINE`
  - secondary CTA は `https://addness.co.jp/`
    - `total_clicks = 33`
  - つまりこの 1 通は
    - `main CTA = 今すぐ反応を取りに行く`
    - `secondary CTA = 会社理解を補助する`
    の 2 層で読む
- `3/10 セミナーPR_3通目 (フリープラン)`
  - `sent = 5,044`
  - `open_rate = 38.87%`
  - `click_rate = 0.654%`
  - main CTA は `direct LINE`
- `3/11 AIキャリアセミナーPR_1通目`
  - `sent = 257,147`
  - `open_rate = 9.58%`
  - `click_rate = 0.045%`
  - main CTA は `direct LINE`
- `3/10 セミナーPR_1通目 (フリープラン)`
  - `sent = 5,012`
  - `open_rate = 38.77%`
  - `click_rate = 1.028%`
  - main CTA は `direct LINE`
- `2/28 AIセミナーPR_2通目`
  - `sent = 256,776`
  - `open_rate = 10.28%`
  - `click_rate = 0.067%`
  - main CTA は `direct LINE`
- `2/28 AIセミナーPR_2通目 (フリープラン)`
  - `sent = 6,493`
  - `open_rate = 45.40%`
  - `click_rate = 1.361%`
  - main CTA は `direct LINE`
- `2/28 AIセミナーPR_1通目 (フリープラン)`
  - `sent = 6,572`
  - `open_rate = 45.63%`
  - `click_rate = 1.942%`
  - main CTA は `direct LINE`
- `2/28 AIセミナーPR_1通目`
  - `sent = 261,029`
  - `open_rate = 10.23%`
  - `click_rate = 0.083%`
  - main CTA は `direct LINE`
- `2/12 AIセミナーオプト`
  - `sent = 282,330`
  - `open_rate = 11.11%`
  - `click_rate = 0.112%`
  - main CTA は `direct LINE`
- `2/12 AIセミナーオプト (フリープラン)`
  - `sent = 5,798`
  - `open_rate = 46.89%`
  - `click_rate = 2.094%`
  - main CTA は `direct LINE`
- `2/11 AIセミナーオプト`
  - `sent = 288,071`
  - `open_rate = 11.55%`
  - `click_rate = 0.112%`
  - main CTA は `direct LINE`

#### 2026-03 中旬 regular campaign の追加 representative

- `AI全自動PR_3通目(3/15)`
  - `sent = 259,401`
  - `open_rate = 5.45%`
  - `click_rate = 0.056%`
  - main CTA は `direct LINE`
  - follow family は `%40631igmlz`
  - 解釈
    - 広い母集団への後半追撃
    - `open_rate` と `click_rate` がともに落ちているので、本文より前に `温度が低い母集団へ後半通を打っている` 可能性を疑う
- `AI全自動PR_1通目(3/13) (copy 01)`
  - `sent = 259,426`
  - `open_rate = 9.10%`
  - `click_rate = 0.251%`
  - main CTA は `direct LINE`
  - follow family は `%40303zgzwt`
  - 解釈
    - 同じ family の 1 通目でも、follow family が変わると click の質が変わりうる
    - `copy` 付きでも `sent > 0` なので current 実績として読む
- `AI全自動PR_1通目(3/13)`
  - `sent = 259,813`
  - `open_rate = 10.05%`
  - `click_rate = 0.213%`
  - main CTA は `direct LINE`
  - follow family は `%40631igmlz`
  - 解釈
    - 広い母集団向け 1 通目としては、3 通目より自然に反応が高い
    - 1 通目と 3 通目を比べる時は、本文だけでなく `送る順番` もセットで見る
- `Resend: AIキャンプ`
  - `sent = 247,861`
  - `open_rate = 5.99%`
  - `click_rate = 0.059%`
  - main CTA は `UTAGE`
  - 解釈
    - `regular campaign = direct LINE` に寄る current 実態はあるが、promotion によっては `UTAGE` 主導も現役で存在する
    - つまり report 読みでは、`regular campaign` という箱だけで CTA family を決め打ちしない

#### regular campaign report を読む時の current 補助基準

- `広い母集団`
  - `sent = 250,000+`
  - `open_rate = 5〜11%`
  - `click_rate = 0.04〜0.25%`
  - まず `件名 / 温度 / CTA family` の順で疑う
- `高温セグメント`
  - `sent = 5,000〜7,000`
  - `open_rate = 38〜47%`
  - `click_rate = 0.65〜2.09%`
  - まず `刈り取りの強さ` と `main CTA の明確さ` を見る
- `copy` や `Resend` の扱い
  - 名前だけで legacy と判断しない
  - `sent > 0` なら current 実績
  - `sent = 0` なら draft 候補
  - `click_rate = 0.211%`
  - main CTA は `direct LINE`
- `本気コンサルPR_1通目_ (2/8)`
  - `sent = 282,022`
  - `open_rate = 10.51%`
  - `click_rate = 0.141%`
  - main CTA は `direct LINE`
- `本気コンサルPR_1通目_ (2/8) (copy 01)`
  - `sent = 5,748`
  - `open_rate = 44.61%`
  - `click_rate = 4.999%`
  - main CTA は `direct LINE`
- `Resend: AIキャンプ`
  - `sent = 247,861`
  - `open_rate = 2.81%`
  - `click_rate = 0.020%`
  - main CTA は `UTAGE`
- `AIキャンプ`
  - `sent = 273,008`
  - `open_rate = 11.07%`
  - `click_rate = 0.069%`
  - main CTA は `UTAGE`

読み方:
- 広い母集団へ打つ全体 campaign は、`open_rate 9〜10% / click_rate 0.04〜0.08%` とかなり薄い
- 既に温度がある `フリープラン` 母集団へ打つ campaign は、`open_rate 38〜45% / click_rate 0.59〜1.94%` と大きく改善する
- `本気コンサルPR_1通目` でも
  - 全体版は `open_rate 10.51% / click_rate 0.141%`
  - `copy 01` の絞り込み版は `open_rate 44.61% / click_rate 4.999%`
  と差が大きい
- `UTAGE` を main CTA にした campaign も sent 実績を持つため、`regular campaign = direct LINE only` と単純化しない
- したがって current regular campaign は、本文の良し悪しだけでなく `誰に打つか` の切り方で反応差が極端に出る
- direct LINE の main CTA でも landing 先は 1 つではない
  - `follow=%40631igmlz`
  - `follow=%40770dyrre`
  - `follow=%40303zgzwt`
  の family が current 実測で混在する
- したがって `direct LINE` と一括りで読まず、`どの LINE family に送っているか` まで見る
- また、main CTA だけでなく secondary CTA の役割も分けて読む
  - 例: `3/11 AIキャリアセミナーPR_2通目`
    - main CTA = `direct LINE` (`149 clicks`)
    - secondary CTA = `https://addness.co.jp/` (`33 clicks`)
  - したがって review 時は
    - `どこが最も押されているか`
    - `2番目の導線が何の補助役割を持つか`
    まで確認する

helper:
- `python3 System/scripts/mailchimp_campaign_snapshot.py --limit 5`
- recent sent regular campaign を API から読み、
  - `title`
  - `subject_line`
  - `emails_sent`
  - `open_rate`
  - `click_rate`
  - `main_cta_type`
  - `main_cta_href`
  を JSON で抜ける
- current 実動作では
  - `3/11 AIキャリアセミナーPR_2通目`
  - `3/10 セミナーPR_3通目 (フリープラン)`
  - `3/11 AIキャリアセミナーPR_1通目`
  - `Resend: AIキャンプ`
  - `AIキャンプ`
  など recent 5 本を再取得できることを確認済み
- `liff-gateway.lineml.jp` も `direct LINE` として分類する

### live login 補助の current 事実

- login 補助は `System/scripts/mailchimp_login_helper.py`
- 2026-03-13 の live で最初の blocker は OneTrust の cookie overlay だった
- helper は現在、最初に `dismiss_cookie_overlay(page)` を通す
- その後に
  - `email`
  - `password`
  - `verify`
  の順に進む
- したがって Mailchimp の live exact 作業では
  - cookie overlay を先に消す
  - `verify` は別段階として扱う
  のが current の正しい読み方

### Addness current CTA rule

- Mailchimp で今後新しく作るメールの `main CTA` は、Journey / Campaign を問わず `short.io` を正とする
- 理由
  - BAN やリンク失効時に設置箇所を触らず差し替えられる
  - URL管理シートで正本管理できる
  - クリック分析を揃えやすい
  - direct LIFF の直貼り事故を減らせる
- `direct LIFF` は例外
  - 一時的なテスト
  - 失効対策も差し替えも分析も不要な特殊ケース
- したがって current に direct LIFF の campaign が残っていても、それは `現状の実装` であり、`今後の標準` ではない

### コンテンツを読む時の基準

- Mailchimp のメールは、単に情報を伝えるものではない
- `コミュニケーションの手段であり、ユーザーをこちらが意図した認識に変換するもの`
  として読む
- Addness の current では、特に次の 3 点で良し悪しを判断する
  - 何の認識を変えたいかが 1 通ごとに明確か
  - 次にしてほしい行動が 1 つに絞られているか
  - 前の接点から promise が切れず、次の接点へ滑らかにつながるか

### 良い / 悪いの判断

#### 良い

- 1 通の役割が明確
  - 例
    - 世界観理解
    - authority 補強
    - urgency 付与
    - 予約促進
- 件名、本文、CTA が同じ方向を向いている
- segment の温度に合っている
  - 全体配信なのに強すぎる刈り取りにしない
  - 温まった segment には回りくどすぎる説明をしない
- 次の接点の文脈を壊さない
  - 広告や LP で約束した promise をメールで引き継ぐ

#### 悪い

- 1 通で教育も予約も販売も全部やろうとする
- 件名で煽り、本文で説明に戻り、CTA で別オファーに飛ばす
- segment の温度と本文の強さがずれる
- 表示URLと実 hyperlink がずれる

### current から読む representative な制作者意図

- `3/11 AIキャリアセミナーPR_1通目`
  - 解釈
    - まだ動いていない広い母集団に対して、まず `これは自分向けの案内だ` と認識させる入口
  - 変えたい認識
    - `よくある案内かも`
    - を
    - `一度見た方がよい案内かもしれない`
    に変える
- `3/11 AIキャリアセミナーPR_2通目`
  - 解釈
    - 1通目で薄く関心を持った相手に、後回しの言い訳を削る役割
  - 変えたい認識
    - `今じゃなくてもいい`
    - を
    - `今回の募集で動く理由がある`
    に変える
- `3/10 セミナーPR_1通目 (フリープラン)`
  - 解釈
    - 既に一定の温度がある母集団へ、オファーとの距離を一気に縮める役割
  - 変えたい認識
    - `少し気になる`
    - を
    - `参加してみる価値が高い`
    に変える
- `3/10 セミナーPR_3通目 (フリープラン)`
  - 解釈
    - 説明より決断促進に寄せる締切 / 後押しメール
  - 変えたい認識
    - `また今度でいい`
    - を
    - `今ここで押さない理由がない`
    に変える

### 実測から読む重要な事実

- 同じ「セミナーPR」でも、全体配信と `フリープラン` segment では開封率もクリック率も大きく違う
- つまり current の Mailchimp では、本文の上手さだけでなく
  - 誰に送るか
  - その人の今の温度は何か
  を切ること自体がコンテンツ設計の一部

### Addness current tag rule

- Mailchimp の tag name は英語で作る
- 基本構造は `media_funnel_event`
- 例
  - `meta_ai_optin`
  - `x_skillplus_webinar1_optin`
  - `linead_sns_consult_reserve`

理由:
- Mailchimp では日本語 tag が消えたり崩れたりしやすい
- 媒体 / ファネル / イベントを英語で固定した方が運用事故が少ない

### 依頼を受けたら最初に確定すべき変数

- Journey か Campaign か
  - evergreen なら Journey
  - 単発 promotion なら Campaign
- audience はどれか
- 誰に送るか
  - trigger tag
  - saved segment
  - exclude 条件
- メールの役割は何か
  - 教育
  - 予約
  - 直オファー
  - 横展開
  - 締切ブースト
- 何を main CTA にするか
  - short.io
  - UTAGE
  - direct LIFF
  - 外部ページ
- 何で状態を作るか
  - tag
  - branch / filter
  - step pause / active
- 何を検証するか
  - 送信数
  - 開封率
  - クリック率
  - click-details
  - 最終遷移先

### 新規設定の標準手順

1. Journey か Campaign かを決める
2. audience と対象条件を決める
   - tag
   - saved segment
   - exclude
3. メールの役割を 1 つに絞る
   - 教育
   - 予約
   - 直オファー
   - 横展開
   - 締切
4. main CTA を 1 本決める
   - 原則は `short.io`
   - `UTAGE` や `direct LIFF` は例外時だけ
5. 本文を入れたら、表示文字列ではなく実 hyperlink を確認する
6. 実際にクリックして、最終遷移先まで確認する
7. 送信後は `送信 -> 開封 -> クリック -> click-details` の順で見る

### AIライティング

- モデル: Claude（Anthropic API）
- インプット: セグメント情報 + 過去の配信実績（開封率・クリック率） + ブランドトーン
- アウトプット: 件名 + プレヘッダー + HTML本文
- テンプレート: 既存のMailchimpテンプレートをベースに本文部分のみAI生成

### 承認フロー

```
Phase 1（初期）: AI下書き → Slack通知 → 甲原承認 → 送信
Phase 2（安定後）: AI下書き → 自動送信（承認率が安定したら移行）
```

### Mailchimp ↔ CDP 双方向同期

Mailchimp側にしかないデータ（開封履歴・クリック履歴・バウンス）をCDPに還流:
- 配信後にレポートAPIで結果取得
- CDPに「メール開封率」「最終クリック日」等のカラムを追加（将来）
- これによりCDP側でさらに精度の高いセグメントが切れる

---

## 実装ファイル構成（予定）

| ファイル | 説明 |
|---------|------|
| `System/cdp_mailchimp.py` | メインスクリプト（同期・セグメント・キャンペーン管理） |
| `System/config/mailchimp.json` | APIキー・サーバー・オーディエンスID（.gitignore対象） |

## 2026-03-10 追加確認

### API接続

- `System/config/mailchimp.json` の API キーで接続確認済み
- account 名は `アドネス株式会社`
- audience / list ID は `82aeac7431`

### current Journey を読む標準手順

1. `GET /customer-journeys/journeys`
   - `journey_name`
   - `status`
   - `stats.started / in_progress / completed`
   を確認する
2. `GET /customer-journeys/journeys/{id}/steps`
   - `step_type = action-send_email`
   - `status = active`
   - `stats.queue_count > 0`
   の step を current 対象として拾う
3. 同じ step の
   - `action_details.email.settings.title`
   - `action_details.email.settings.subject_line`
   - `action_details.email.emails_sent`
   を見て、メール名・件名・送信実績を読む
4. `GET /campaigns/{campaign_id}/content`
   で本文 HTML を取得し、UTAGE / short.io / その他 CTA を抜く
5. `GET /reports/{campaign_id}/click-details`
   で、実際に押されている URL を確認する

#### Journey を作る時の exact チェック

- `Journey`
  - evergreen で流す理由を 1 文で説明できるか
- `Audience`
  - 先に切れているか
- `Tag added`
  - 何の state を作る trigger なのか明確か
- `Filter who can enter`
  - trigger の代わりに使っていないか
- `Email name`
  - 運用者が見て step の役割をすぐ分かるか
- `Subject line`
  - この 1 通で変えたい認識が分かるか
- 本文
  - 前の step の文脈を引き継いでいるか
- `main CTA`
  - 原則 `short.io`
  - 例外判断なら理由を説明できるか
- `hyperlink`
  - 表示文字列ではなく actual URL が意図どおりか

保存前に最低限やる順番:

1. `Journey` である理由を言えるか
2. `Audience` と `Tag added` を説明できるか
3. 必要なら `Filter who can enter` の条件を説明できるか
4. この step の役割が 1 つに寄っているか
5. `main CTA` の actual hyperlink と final destination を確認したか

current の review で特に落としやすい点は次。

- `Tag added` と `Filter who can enter` の役割が混ざる
- step 単体では良くても、前後の flow と文脈が切れている
- 本文上は short.io っぽく見えるが actual hyperlink が別 URL のまま
- `short.io` は正しいが、その先の `UTAGE / LINE / 外部ページ` が古い
- exploratory draft を放置して builder に残す

#### Campaign を作る時の exact チェック

- `Campaign`
  - 単発配信である理由を 1 文で説明できるか
- `Audience`
  - `Saved segment`
  - `Tag`
  - `Exclude`
  のどれで切るか先に決めているか
- `Subject line`
  - 今回 1 通で変えたい認識が分かるか
- `Preview text`
  - 件名の promise を補強しているか
- 本文冒頭
  - 誰向けか
  - 何の話か
  が数行で伝わるか
- `main CTA`
  - 原則 `short.io`
  - 例外なら理由を説明できるか
- `hyperlink`
  - 表示文字列ではなく actual URL が意図どおりか

保存前に最低限やる順番:

1. `Campaign` である理由を言えるか
2. `Audience` と `Saved segment / Tag / Exclude` を説明できるか
3. `Subject line` と `Preview text` が同じ promise を向いているか
4. 本文冒頭と main CTA の文脈がつながっているか
5. `main CTA` の actual hyperlink と final destination を確認したか

送信前の最終検証順:

1. `Preview and test` で subject line と preview text の見え方を確認する
2. main CTA の hyperlink を editor 上で確認する
3. テスト送信できるなら、自分宛てに送り actual click を確認する
4. `short.io` を使う場合は、short.io の先の最終遷移先まで確認する
5. 補助リンクがあるなら、main CTA と混同されないことを確認する
6. segment / tag / exclude が今回の配信意図と一致することを最後に見直す

ここで止めて確認する条件:

- subject line と本文冒頭の promise がずれている
- 本文上は short.io に見えるのに actual hyperlink が別 URL のまま
- main CTA と補助リンクの優先順位が曖昧
- 送るべき相手が `segment / tag / exclude` のどれで切れているか説明できない

current の review で特に落としやすい点は次。

- `Saved segment` で切るべきところを `Tag` で無理に吸収する
- `Subject line` と本文冒頭の約束がズレる
- 本文上は short.io っぽく見えるが actual hyperlink が別 URL のまま
- `main CTA` が複数あり、何を押してほしいかが曖昧

#### report を読む時の exact チェック

- まず `Journey report` か `Campaign report` かを分ける
- `Open rate`
  - `Click rate`
  を単独で見ず、配信対象の温度感とセットで読む
- `click-details`
  を開き、main CTA と実際に押された URL が一致しているか確認する
- `top_clicked_url`
  だけで判断せず、上位クリックが何本に割れているかを見る
- regular campaign は `本文先頭の href` より `click-details` を優先する

レビュー後に最低限やる順番:

1. 送信母数が想定どおりか
2. `Open rate` が狙った温度感に対して妥当か
3. `click-details` で main CTA が押されているか
4. 補助リンクに click が流れすぎていないか
5. 次回の改善対象を
   - 件名
   - 本文冒頭
   - main CTA
   のどこに置くか決める

ここで止めて確認する条件:

- `Open rate` と `Click rate` のどちらを改善すべきか判断できない
- `click-details` 上位 URL の意味が説明できない
- `main CTA` と実際の top click が一致しない
- short.io の click と campaign report の click が大きくズレる

### current 判定ルール

- `last_started_at` だけで current 判定しない
- `journey が sending` でも `step が paused` なら current メール扱いしない
- `queue_count > 0` を最優先で見る
- `copy` が付くメールでも queue が残っていれば current とみなす
- current 実例
  - `スキルプラスフリープラン1通目 (copy 03)` は `queue = 191 / sent = 31,173`
  - `センサーズFB広告_本気コンサルオファー (copy 05)` は `queue = 1 / sent = 17,987`
  - `AIカレッジFB広告_個別3日間シナリオ_3日目 (copy 02)` は `queue = 1 / sent = 28,146`
  - つまり `copy` は名前上の派生を意味するだけで、current / legacy の判定基準にはならない

### helper の current 状態

- `python3 System/scripts/mailchimp_journey_snapshot.py --journey-id 10934`
  - `--journey-id` は int/string 比較ズレを修正済み
  - 出力に
    - `email_step_summary`
    - `current_email_steps`
    - `email_steps[].state_reason`
    が入る
- `python3 System/scripts/mailchimp_journey_snapshot.py --list-current --count 2`
  - `in_progress` の大きい sending journey を優先して scan する
  - `current_rows` と `paused_with_queue_rows` を横断で返す
- `state_reason` は
  - `current`
  - `queue_zero`
  - `paused_with_queue`
  - `step_not_active`
  - `journey_not_sending`
  の5種
- 代表実測
  - `10934 / UTAGE_AIカレッジ_Facebook_7桁オプトイン2025-10-15`
  - `total = 36`
  - `current = 12`
  - `queue_zero = 24`
  - つまり `active` という表示だけでは current と言えず、`queue_count > 0` まで見ないと誤読する
  - `10932 / UTAGE_センサーズ_Facebook_７桁オプトイン 2025-10-15`
  - `started = 18,733 / in_progress = 4,880 / completed = 13,852`
  - queue-positive current は
    - `本気コンサルRe`
    - `スキルプラスフリープラン1通目 (copy 02)`
    - `スキルプラスフリープラン2通目 (copy 02)`
    - `スキルプラスフリープラン3通目 (copy 02)`
    - `スキルプラスフリープラン4通目 (copy 03)`
  - 一方で `SNS_Meta広告_7桁_1通目` や `センサーズFB広告_本気コンサルオファー (copy 05)` は `active` でも `queue_count = 0`
  - したがって `copy` の有無や `active` 表示だけで current / legacy を判定しない

### regular campaign helper の current 状態

- `python3 System/scripts/mailchimp_campaign_snapshot.py --campaign-id e836af06b7`
  - `GET /reports/{campaign_id}/click-details` も集計する
  - `top_clicked_url` と `top_clicked_total_clicks` を返す
- 代表実測
  - `e836af06b7 / 3/11 AIキャリアセミナーPR_2通目`
  - `main_cta_href = direct LINE`
  - `top_clicked_url = direct LINE`
  - `top_clicked_total_clicks = 145`
  - `https://addness.co.jp/` は副次クリックで `32`
- つまり regular campaign は `本文先頭の href` だけでなく、`click-details` の実クリック URL まで見て主 CTA を判断する

### live UI で確認できたこと

- ログイン導線は `https://login.mailchimp.com/` -> `https://login.mailchimp.com/login/unifiedLoginPost/` -> `https://us5.admin.mailchimp.com/login/verify/` の順
- `System/scripts/mailchimp_login_helper.py` で
  - 既存 Chrome セッションの再利用
  - `mailchimp_login.json` からの email / password 自動入力
  までは固定済み
- `System/scripts/mailchimp_tfa_code_helper.py` で
  - `line_bot_local/config.json` の `server_url + agent_token` を使い
  - `/api/group-log` から `Mailchimp verification code is: 123456` の 6桁コードを抽出できる
  - `--wait-seconds` を付けると fresh code polling もできる
- `mailchimp_login_helper.py` の current 実装では
  - `Send code via SMS`
  - 最新コードの取得
  - verify 入力
  - `Verify / Continue / Log in`
  まで自動で試す
- `Playwright.connect_over_cdp` が timeout する回は、helper は raw CDP fallback に切り替わる
- raw fallback の current 到達点は
  - `2-factor authentication | Mailchimp`
  - `https://us5.admin.mailchimp.com/login/tfa-post`
  まで
- `login/verify` の current 手順は
  - `Send code via SMS`
  - LINE の `Mailchimp認証` グループでコード確認
  - verify 画面へ入力
  の順
- 2段階認証コードは、少なくとも current 運用では `Mailchimp認証` LINE グループで確認する前提
- historical group-log では
  - `2026-03-15T03:31:17`
  - `2026-03-14`
  - `2026-03-13`
  の Mailchimp 認証コード message を取得できた
- つまり current の残差は `認証コードの取得` ではなく、`fresh code polling 後に verify を closing まで通す exact 性`
- helper は 2026-03-17 時点で hanging せず `LOGIN_NEEDS_TFA / login/tfa-post` を返すところまでは安定
- `login/verify` 画面では
  - `Send code via email`
  - `Send code via SMS`
  - `Remember this device`
  が visible
- Automations 一覧には
  - `Build from scratch`
  - `Choose flow template`
  - `Search by name`
  - `Status`
  - `Objective`
  - `Sort by: Last created`
  がある
- `Build from scratch` を押すと `customer-journey/create-new-journey/` に入り、
  - `Name flow`
  - `Audience`
  - `Choose a trigger`
  の順で新規 flow を作る
- `Choose a trigger` を押すと、その時点で
  - route が `customer-journey/builder?id={id}&stepModal=trigger`
  に変わり、draft flow が作られる
- `Build from scratch` は、フロー名を入れて trigger を選び始めた時点で draft を作成する
- そのため live テスト時は
  - 仮名で確認
  - 必要な UI を確認
  - 一覧へ戻って `Actions -> Delete -> Delete flow`
  で同セッション内に削除する
- Journey 名クリックで `https://us5.admin.mailchimp.com/customer-journey/builder?id={id}` に入る
- builder 画面の current 操作は
  - `Send Test Emails`
  - `View Report`
  - `Pause & Edit`
  - `Save and close flow`
  - `Contact tagged ...`
  - `Filter who can enter`
  - `Manage Step`
  を軸に見る
- builder の右側は `Data / Settings` 切り替えで、flow summary と設定確認を行う
- `Tag added` を trigger に選ぶと
  - `Set a tag`
  - `Filter who can enter`
  - `Save Trigger`
  が表示される
- `View Report` は `https://us5.admin.mailchimp.com/customer-journey/report?id={id}` に遷移する
- current report 画面では少なくとも
  - `Days active`
  - `Total started`
  - `Total in progress`
  - `Total completed`
  - `Open rate`
  - `Click rate`
  - `Unsubscribe rate`
  - `Delivery rate`
  を flow 単位で読む
- 代表例 `UTAGE_AIカレッジ_Facebook_7桁オプトイン2025-10-15` report では
  - `Days active = 147`
  - `Total started = 32,167`
  - `Total in progress = 4,596`
  - `Total completed = 27,567`
  - `Open rate = 17.4%`
  - `Click rate = 0.04%`
  を確認済み
- trigger picker では
  - `Popular`
  - `Contact Activity`
  - `Date & Special Events`
  - `Shopping Activity`
  - `Marketing Activity`
  - `Booking Activity`
  - `Payment Activity`
  - `API & Integrations`
  のカテゴリが見える
- Campaign Manager 一覧は `https://us5.admin.mailchimp.com/campaigns/`
  - `Name / Status / Audience / Analytics / Actions` を一覧で見る
  - row 上で `Regular email` `Automation flow` `Draft / Sent` が visible
  - segment を切った regular email では row 上に `Exclude: ...` や `Segment: ...` が出る
- Campaign の create 入口は current UI では `campaigns/#/create-campaign`
  - `Create` から SPA 的に入る
  - `campaigns/create/` 直URLでは 404 になる
  - つまり current の 1回配信は `Campaign Manager -> Create` から入る前提で見る
- regular campaign の current 作成順は、少なくとも次で固定して読む
  1. `Campaign Manager`
  2. `Create`
  3. `Regular email`
  4. `Audience`
  5. `Segment or Tag / Exclude`
  6. `Campaign name`
  7. `Subject line`
  8. `Preview text`
  9. `From name`
  10. `Reply to`
  11. `Design Email` または本文編集
  12. `Review`
  13. `Send` または `Schedule`
- つまり regular campaign では、本文作成より先に
  - `誰に送るか`
  - `何の役割の1通か`
  を固定しないと current の quality に寄らない
- API でも `regular campaign` は create / content / delete まで通る
  - `POST /campaigns` で draft 作成
  - `PUT /campaigns/{id}/content` で本文設定
  - `DELETE /campaigns/{id}` で cleanup
  - 2026-03-11 に `ZZ_TEST_DELETE_20260311` で create -> content -> delete を実行し、最終的に `404` になるところまで確認済み

### current 代表 Journey

- `UTAGE_AIカレッジ_Facebook_7桁オプトイン2025-10-15`
- `UTAGE_センサーズ_Facebook_７桁オプトイン 2025-10-15`
- `UTAGE_AIカレッジ_X広告_7桁オプトイン`
- `UTAGE_AI_YouTubeメインファネル_7桁オプトイン`

helper:
- `python3 System/scripts/mailchimp_journey_snapshot.py --query UTAGE_AIカレッジ_Facebook_7桁オプトイン2025-10-15`
- Journey 1本の
  - `status`
  - `started / in_progress / completed`
  - trigger type
  - active email step
  - `queue_count`
  - `emails_sent`
  を JSON で抜ける
- current 実動作では、AI Facebook 7桁本線で
  - trigger = `trigger-tag_added`
  - `AI導入プロコンサル = queue 2`
  - `スキルプラスフリープラン2通目 = queue 228`
  - `完全攻略合宿販売期間延長 = queue 196`
  を再取得できた
- `UTAGE_センサーズ_Facebook_７桁オプトイン 2025-10-15` の current 実測
  - `started = 18,733`
  - `in_progress = 4,880`
  - `completed = 13,852`
  - `current = 5`
  - `paused_with_queue = 1`
  - queue-positive active は
    - `本気コンサルRe`
    - `スキルプラスフリープラン1〜4通目 (copy 02/03)`
  - 一方で
    - `センサーズFB広告_本気コンサルオファー (copy 05)`
    - `センサーズFB広告_7桁オプトインシナリオ1〜8通目 (copy 05)`
    は `active` でも `queue_count = 0`
  - つまり `copy` が付くかではなく
    - `status = active`
    - `queue_count > 0`
    - `emails_sent`
    の組み合わせで current を読む
- `UTAGE_AIカレッジ_X広告_7桁オプトイン` の current 実測
  - `started = 27,571`
  - `in_progress = 2,287`
  - `completed = 25,281`
  - `current = 26`
  - queue-positive active は
    - `スキルプラスフリープラン1〜5通目`
    - `AIカレッジX広告_メインシナリオ2 / 3 / 5 / 7通目`
    - `AI習得レッスン2〜5日目`
    - `プロンプト企画2〜3日目`
    - `SNS企画2〜3日目`
    - `個別誘導1〜7通目`
  - 一方で
    - `AIカレッジX広告_メインシナリオ1通目`
    - `AIカレッジX広告_プチセミナー (プロコンサル)`
    - `AIカレッジX広告_完全攻略合宿販売期間延長`
    は `active` でも `queue_count = 0`
  - つまり current Journey の中でも
    - `今も送信待ちが残る枝`
    - `もう走り切った枝`
    が混在する

### current 代表メール

- AI Facebook
  - `スキルプラスフリープラン1〜5通目`
  - `AIカレッジFB広告_プロンプト企画`
  - `AIカレッジFB広告_SNS企画`
  - `フリープラン岡田結実`
- SNS Facebook
  - `本気コンサルRe`
  - `スキルプラスフリープラン1〜5通目`
- AI X
  - `スキルプラスフリープラン`
  - `AI習得レッスン`
  - `プロンプト企画`
  - `個別誘導`

### current メールの作り方と製作者の意図

メール本文は、件名だけでなく `何の心理状態を作るか` で分けて読む方がズレにくい。current の代表型は大きく 5 つある。

#### 1. 本線ストーリー型

- representative
  - `SNS_Meta広告_7桁_1通目`
- 役割
  - みかみ本人の人物像と authority を先に入れて、読み手との距離を縮める
- 本文の動き
  - `どこで僕を知ってくれたか分からないので改めて...`
  - `東大中退 / 20億企業社長` などの肩書き
  - 外部動画やチャンネル実績
  - 最初の CTA
- 製作者の意図
  - いきなり売るのではなく、まず `この人の話なら読んでいい` を作る
  - main CTA を押さなくても、以降の nurture を読む理由を作る
- 良い理由
  - authority と story が先にあり、読者が「誰からのメールか」を理解しやすい
  - CTA は前に出しすぎず、教育導線の入口として置かれている

#### 2. 直オファー型

- representative
  - `センサーズFB広告_本気コンサルオファー (copy 05)`
  - `AIカレッジFB広告_AI導入プロコンサル (copy 02)`
- 役割
  - `今なら無料 / モニター募集 / 本気の人限定` の強い口実で個別相談へ寄せる
- 本文の動き
  - 冒頭で offer を一言で出す
  - `以前からたくさん声をもらっていた`
  - `精鋭コンサルティング部隊`
  - `6ヶ月後の売上が一桁変わるビジョン`
  - 誰に向いているかを箇条書き
  - CTA を 1 本に絞る
- 製作者の意図
  - 教育よりも予約率を優先し、`今押す理由` を作る
  - 「無料」「限定」「思いつき」の軽さで心理ハードルを下げる
- 良い理由
  - オファーが先、対象者定義が次、CTA が最後で、読む順番が明確
  - short.io が 1 本にまとまっていて、行動先が迷わない

#### 3. 横展開オファー型

- representative
  - `スキルプラスフリープラン1通目 (copy 03)`
- 役割
  - AI / SNS の本線読者を、スキルプラスの無料枝へ横展開する
- 本文の動き
  - `お金がなくて挑戦できない` という反論処理
  - `テレビCM / 累計4000名 / 林社長推薦` で社会的証明
  - `0円スキルGETプログラム`
  - 無料だが一部制限ありと明示
  - CTA を複数回差し込む
- 製作者の意図
  - 本線で温まった読者に対して、別商品ではなく `無料で入れる次の選択肢` を提示する
  - 費用障壁を崩して、接点数を増やす
- 良い理由
  - 反論処理 -> 権威付け -> 新オファー提示の順で自然
  - 既存の nurture から無理なくスキルプラス枝へ送れる

#### 4. 短期締切ブースト型

- representative
  - `AIカレッジFB広告_プロンプト企画3日目 (copy 02)`
  - `本気コンサルRe`
- 役割
  - すでに説明済みの offer に対して、期限と実例で最後の一押しをする
- 本文の動き
  - 冒頭で `本日23:59` などの締切
  - 受講者の声や具体例
  - いま参加しないデメリット
  - CTA の再提示
- 製作者の意図
  - 読者の迷いを切り、`今動く理由` を明確にする
  - 途中で開封した人を再点火する
- 良い理由
  - 既に理解している前提で書かれていて、説明が冗長でない
  - 実例と締切の組み合わせで、行動トリガーが強い

#### 5. 単発PR告知型

- representative
  - `3/11 AIキャリアセミナーPR_1通目`
  - `3/11 AIキャリアセミナーPR_2通目`
  - `3/10 セミナーPR_1通目 (フリープラン)`
- 役割
  - 今この期間だけ打ちたい promotion を、特定 segment または広い母集団へ一気に届ける
- 本文の動き
  - 件名で強く attention を取りにいく
  - 冒頭で対象者や問題を限定する
  - 今回の企画や offer を短く提示する
  - CTA はすぐ押せる位置に置く
- 製作者の意図
  - Journey のような長い教育ではなく、既存認知を前提に短く反応を取る
  - 期間限定やイベント告知で、判断を後ろ倒しにさせない
- 良い理由
  - `今の告知` に必要な情報だけへ絞りやすい
  - segment を切れば、広い配信より開封率とクリック率を上げやすい

### 良いコンテンツと悪いコンテンツの判断

- 良い
  - メールの役割が `教育 / 予約 / 横展開 / 締切ブースト / 単発PR` のどれかに明確に振り切れている
  - CTA が 1 本または少数に絞られている
  - subject, body, CTA の約束が一致している
  - 「誰に向けたメールか」が本文前半で分かる
  - ハイパーリンクを使う場合でも、表示文字列と実際の埋め込み URL が一致していることを配信前に確認している
- 悪い
  - 1 通の中に `教育 / オファー / 別商品案内` を詰め込みすぎる
  - authority を積みすぎて、何をしてほしいかが遅い
  - CTA が複数方向に割れている
  - 期限訴求だけ強く、文脈や対象者定義が薄い
  - 表示文字列の URL を見て安心し、実際のリンク先を押して確認していない

### 配信後レポートの読み方

current の representative report を読むと、見る順番は次で固定するとズレにくい。

1. `emails_sent`
2. `opens.opens_total / opens.unique_opens / opens.open_rate`
3. `clicks.clicks_total / clicks.unique_subscriber_clicks / clicks.click_rate`
4. `click-details`
5. click 先が `short.io` なら short.io 側、`UTAGE` なら UTAGE 側へ進む

#### 30秒レビューの順番

1. `emails_sent`
2. `open_rate`
3. `click_rate`
4. `click-details`
5. `main CTA`
6. `secondary CTA`
7. downstream を次にどこまで見るか

#### representative report を読む時の問い

- この report は `広い母集団の反応確認` か、`高温セグメントの刈り取り確認` か
- `main CTA` と `secondary CTA` を分けて説明できるか
- `click-details` の上位 URL は、配信前に意図した導線と一致しているか
- 次に見るべき downstream は `short.io / UTAGE / direct LINE` のどれか
- rate の問題なのか、送付対象の relevance の問題なのかを切れているか

#### representative

- `センサーズFB広告_本気コンサルオファー (copy 05) / 48ed0b9c61`
  - `emails_sent = 18,006`
  - `unique_opens = 3,015`
  - `open_rate = 16.92%`
  - `unique clicks = 53`
  - main click は `https://skill.addness.co.jp/metaad-sns5`
- `AIカレッジFB広告_AI導入プロコンサル (copy 02) / 9da38c6346`
  - `emails_sent = 31,629`
  - `unique_opens = 7,227`
  - `open_rate = 23.09%`
  - `unique clicks = 41`
  - main click は `https://skill.addness.co.jp/meta-ai4`
- `スキルプラスフリープラン1通目 (copy 03) / 3a34a0bd07`
  - `emails_sent = 31,173`
  - `unique_opens = 7,177`
  - `open_rate = 23.25%`
  - `unique clicks = 69`
  - main click は `https://school.addness.co.jp/p/TzYRwepfqzFq?...`
- `3/11 AIキャリアセミナーPR_2通目 / e836af06b7`
  - `emails_sent = 255,250`
  - `open_rate = 9.51%`
  - `click_rate = 0.04%`
  - main click は direct LIFF
- `3/11 AIキャリアセミナーPR_1通目 / d0ebb7f000`
  - `emails_sent = 257,147`
  - `open_rate = 9.29%`
  - `click_rate = 0.04%`
  - main click は direct LIFF
- `3/10 セミナーPR_3通目 (フリープラン) / f240d66379`
  - `emails_sent = 5,044`
  - `open_rate = 38.07%`
  - `click_rate = 0.57%`
  - main click は `follow=@076cqpuk` の direct LIFF
- `3/10 セミナーPR_1通目 (フリープラン) / 25a53347a3`
  - `emails_sent = 5,012`
  - `open_rate = 38.55%`
  - `click_rate = 1.03%`
  - main click は `follow=@076cqpuk` の direct LIFF

上の current regular campaign 4 本から分かること:

- 広い母集団への単発配信は `開封 9%台 / クリック 0.04%台`
- フリープラン絞り込みの単発配信は `開封 38%台 / クリック 0.5〜1.0%台`
- つまり Campaign は、segment の切り方で成果差がかなり大きくなる

#### 2026-02-28 から 2026-03-11 の recent sent regular で見えた current 実測

- 広い母集団への campaign
  - `3/11 AIキャリアセミナーPR_2通目`
    - `sent 255,250 / open_rate 9.66% / click_rate 0.04%`
  - `3/11 AIキャリアセミナーPR_1通目`
    - `sent 257,147 / open_rate 9.33% / click_rate 0.04%`
  - `2/28 AIセミナーPR_2通目`
    - `sent 256,776 / open_rate 10.26% / click_rate 0.07%`
  - `2/28 AIセミナーPR_1通目`
    - `sent 261,029 / open_rate 10.21% / click_rate 0.08%`
- フリープラン絞り込み campaign
  - `3/10 セミナーPR_3通目 (フリープラン)`
    - `sent 5,044 / open_rate 38.15% / click_rate 0.57%`
  - `3/10 セミナーPR_1通目 (フリープラン)`
    - `sent 5,012 / open_rate 38.57% / click_rate 1.03%`
  - `2/28 AIセミナーPR_2通目 (フリープラン)`
    - `sent 6,493 / open_rate 45.35% / click_rate 1.33%`
  - `2/28 AIセミナーPR_1通目 (フリープラン)`
    - `sent 6,572 / open_rate 45.58% / click_rate 1.94%`
- 解釈
  - current の regular campaign は、母集団の relevance で結果差がかなり大きい
  - 単発配信の精度を上げる時は、件名より先に `誰に送るか` を疑う方が筋が良い

#### click-details の注意

- 同じ URL が複数行に見えることがある
- これは `同一リンクが壊れている` という意味ではなく、同じ遷移先を本文内の複数箇所に配置しているため
- つまり、Mailchimp の click-details は
  - `どの URL が押されたか`
  だけでなく
  - `その URL を何箇所に置いていたか`
  の影響も受ける
- 実務では
  - まず `main CTA の URL`
  - 次に `会社概要 / unsubscribe` などの補助リンク
  を分けて見る
- current では、Journey 本線は short.io や UTAGE ページへ送る例が多い一方、regular campaign は direct LIFF が main CTA の例もある
- したがって `click-details -> short.io -> UTAGE` だけで固定せず、campaign type ごとに main CTA の型を見分ける
- ただし今後こちらが新規で組む場合は、campaign でも `main CTA = short.io` を標準にする
- 2026-03-16 の recent 10 件 snapshot では
  - `main CTA = direct LINE`: `10 / 10`
  - `hyperlink_mismatch_count = 0`: `10 / 10`
  だった
- つまり recent regular campaign の実態は `direct LINE` 優勢だが、recent 実測では link mismatch は出ていない

### current / legacy copy の読み方

- `copy` が付く = legacy ではない
- まず見るべきは
  - `journey status`
  - `step status`
  - `queue_count`
  - `emails_sent`
- `copy` でも
  - queue が残り
  - sent が積み上がり
  - click report に short.io / UTAGE の current CTA が出ていれば
  current として扱う
- Campaign でも
  - `status = sent`
  - `emails_sent > 0`
  - click report に main CTA が出ていれば
  `copy` 付きでも current 実績として扱う
- 逆に `copy` でなくても
  - paused
  - queue 0
  - click が枯れている
  なら current とは限らない

### current / legacy の判断辞書

まず `名前` ではなく `挙動` で切る。current の読み方は次の 4 区分がズレにくい。

#### 1. current

- 条件
  - `journey = sending`
  - `step = active`
  - `queue_count > 0`
- 読み方
  - 今もこの step に入る人が残っていて、送信待ちがある
- representative
  - `スキルプラスフリープラン1通目 (copy 03) = queue 191 / sent 31,173`
  - `センサーズFB広告_本気コンサルオファー (copy 05) = queue 1 / sent 17,987`
  - `AIカレッジFB広告_個別3日間シナリオ_3日目 (copy 02) = queue 1 / sent 28,146`
  - `AI新卒プロモーション_1通目(4/3) (copy 02) = sent 269,919 / open_rate 10.85% / main click は direct LIFF`
  - `Resend: AIキャンプ = sent 247,861 / main click は UTAGE page`

#### 2. completed but still valid

- 条件
  - `step = active`
  - `queue_count = 0`
  - `emails_sent` は十分ある
- 読み方
  - その step 自体は current の構成要素だが、今この瞬間の送信待ちはない
  - evergreen の末尾や、最近ちょうど queue が尽きた step に起こる
- 扱い
  - legacy と決めつけず、同 Journey の前後 step と click report を見る
- representative
  - `フリープラン_入会完了メール = active / queue 0 / sent 2,152`
  - `AIカレッジFB広告_メインシナリオ1通目 (copy 05) = active / queue 0 / sent 32,169`
  - `SNS_Meta広告_7桁_1通目 = active / queue 0 / sent 18,369`

#### 3. historical evidence

- 条件
  - `step = paused`
  - `emails_sent > 0`
- 読み方
  - 過去に実運用された証拠はあるが、今は止めている
- 扱い
  - content pattern や historical reference としては価値がある
  - current 修正対象には入れない
- representative
  - `月額プラン2通目 = paused / queue 3,823 / sent 170`
  - `止める = paused / queue 740 / sent 3,823`
  - `送信停止 = paused / queue 2,821 / sent 42,290`
- 補足
  - `paused` なのに `queue_count > 0` が残る step もある
  - これは `今も current` の意味ではなく、停止時点で queue が残った historical remnant として読む方が安全

#### 4. draft / legacy candidate

- 条件
  - `step = paused`
  - `queue_count = 0`
  - `emails_sent = 0` または click report が枯れている
- 読み方
  - current 本線から外れている可能性が高い
- 扱い
  - ただし `copy` や古い命名だけで断定せず、Journey 全体の status と周辺 step を確認する
- representative な見方
  - `paused + queue 0 + sent 0` は、まず draft / unused 候補
  - `paused + queue 0 + sent が極小` でも、同 Journey に current step が並ぶなら historical 残骸の可能性がある
  - つまり最後は `Journey 全体の status` と `前後 step の queue / sent` を必ず合わせて見る

#### Campaign 側の current / draft 判定

- current に近い
  - `type = regular`
  - `status = sent`
  - 日付付きタイトルや promotion 文脈が current 施策と一致
- draft / pending
  - `type = regular`
  - `status = save`
  - `emails_sent = 0`
- representative
  - `3/11 AIキャリアセミナーPR_1通目 (copy 02) = save / sent 0`
  - `Resend: AIキャンプ (copy 01) = save / sent 0`
  - `save + sent 0 + click 0` なら、配信候補や draft として読む
- 注意
  - `copy` が付いていても、それだけで old と決めない
  - ただし `save + sent 0` の段階では、まだ current 実績とは分ける
  - historical な `regular campaign` では direct LIFF を main CTA にしていることがある
  - ただし今後の新規実装では、campaign でも `main CTA = short.io` を正にする
  - `Resend` という名前も、それだけでは legacy 根拠にならない
    - 実際に `Resend: AIキャンプ` は `sent 247,861` の current 実績を持っていた

### 何が良い判定で、何が危険か

- 良い
  - `queue_count > 0` を最優先で見る
  - `copy` の有無より `step status / sent / click` を優先する
  - current 修正対象は `active + queue > 0` に絞る
- 悪い
  - `last_started_at` だけで current と決める
  - `copy` が付いているから old と決める
  - `step = active` だけで今も送られていると決める
- NG
  - paused step を current と誤認して本番修正する
  - sent が大きいだけで今も使っていると断定する

### 顧客行動データとして追う指標

Mailchimp 単体で閉じず、導線全体では次の 5 点を 1 本で追う必要がある。

1. メール送信数
2. 開封数 / 開封率
3. リンククリック数 / クリック率
4. 遷移先 UTAGE ページの訪問者数
5. 遷移先ページでのコンバージョン数

この 5 点をつなげて初めて、どこで離脱しているかを判断できる。

### 動線データ基盤の目的

CDP にそのまま入れず、別の `動線データ` として持つ前提で考える。  
第一目的は `分析用の可視化` と `マーケティング施策立案のためのデータ収集`。

スキルプラス事業でまず見るべき主要変数は次の 4 つ。

1. CPA
2. 個別相談数
3. 営業成約率
4. 解約率

この 4 変数を改善するために、`送信 -> 開封 -> クリック -> 訪問 -> 行動` の導線データを別レイヤーで持つ。

### current のコンテンツ構造と制作者の意図

コンテンツとは、`コミュニケーションの手段であり、ユーザーをこちらが意図した認識に変換するもの`。

Mailchimp では、この認識変換を `非リアルタイムの長文教育 / 心理変容 / オファー提示` で担う。

Mailchimp の役割は、単にメールを送ることではない。

- まだ冷たい、または半信半疑の見込み客に対して
- ストーリー、authority、実例、緊急性を使い
- `理解している / 信じられる / 今動く理由がある`
  状態まで温度を上げる

つまり Mailchimp は、Addness の導線の中では `認識と温度を段階的に変える本線` として読む。

したがって良い Mailchimp コンテンツは、
- 何を教えるか
- 何を信じさせるか
- 次に何を押してほしいか
が 1 通ごとに明確で、前後のメールとつながっている。

Mailchimp の current メールは、単に件名を量産しているのではなく、かなり明確な役割分担で作られている。

#### 1. 本線 7桁オプトインの役割

- 代表例
  - `AIカレッジFB広告_メインシナリオ1通目 (copy 05)`
  - `SNS_Meta広告_7桁_1通目`
- 共通する意図
  - いきなり売らず、まず `みかみ` の人物ストーリーと authority を入れる
  - 「第1話 / 第2話 ...」で open loop を作り、連続閲覧を促す
  - 広告で感じた期待を `自分にもできそう` に変える
- current で見えている CTA
  - AI 本線 1通目は `https://skill.addness.co.jp/meta-ai2`
  - SNS 本線 1通目は `https://skill.addness.co.jp/metaad-sns2`
- 本文で実際にやっていること
  - AI 本線 1通目は `東大生社長みかみがAI未経験から年収2億ベースで稼ぐまでの軌跡` を主題にして、人物ストーリーと実績で期待を維持している
  - SNS 本線 1通目は `元東大生社長みかみが年商2億稼ぐまでの軌跡` を主題にして、動画やストーリーを複数差し込みながら `もっと見たい` 状態を作っている
- 良いと判断できる点
  - subject が `ストーリー / 権威 / 続き` を担っている
  - 本文は長文でも、役割が `売る前の期待維持` なので整合している

#### 2. 途中の直オファー系メールの役割

- 代表例
  - `AIカレッジFB広告_AI導入プロコンサル (copy 02)`
  - `センサーズFB広告_本気コンサルオファー (copy 05)`
- 共通する意図
  - 本線で温めた読者へ、急に距離を縮める
  - `緊急告知 / 直接コンサル` のラフさで、堅い売り込み感を下げる
  - 個別相談やコンサルへの遷移を強く取りにいく
- current で見えている CTA
  - AI 側は `https://skill.addness.co.jp/meta-ai4`
  - SNS 側は `https://skill.addness.co.jp/metaad-sns5`
- 本文で実際にやっていること
  - AI 側は `【緊急告知！！】直接コンサルしましょうか？笑` というラフな件名で入り、個別に近い距離感を先に作ってから CTA へ寄せている
  - SNS 側の `本気コンサルRe` は `間も無く締め切ります` で urgency を前面に出し、迷っている人を押し込む役割が強い
- 良いと判断できる点
  - 件名と本文のトーンが `思いつき感 / 個別感` に寄っていて、直売りメール特有の警戒を下げている
  - 本線メールと比べて CTA がかなり明確

#### 3. スキルプラス横展開メールの役割

- 代表例
  - `スキルプラスフリープラン1通目 (copy 03)`
  - `スキルプラスフリープラン1通目 (copy 02)`
- 共通する意図
  - AI / SNS で集めた人に対し、別の価値提供口としてスキルプラスを差し込む
  - `無料 / 期間限定 / 挑戦しやすさ` を前面に出し、より広い層へ口を広げる
- current で見えている CTA
  - AI 側は `https://school.addness.co.jp/p/TzYRwepfqzFq?...`
  - SNS 側は `https://school.addness.co.jp/p/hNuqvGx9VJgZ?...`
- 本文で実際にやっていること
  - SNS 側 2通目は `たった10時間で1,300名が押し寄せた...無料のアレの正体とは？` という件名で、無料性と social proof を使って参加ハードルを下げている
  - AI / SNS ともに、高単価の直接訴求ではなく `無料で入れる別の価値提供口` として差し込んでいる
- 良いと判断できる点
  - 本線のストーリーで温度を上げた後に、低いハードルの offer を置いている
  - いきなり高単価へ行かず、無料体験やフリープランを挟んでいる

#### 4. 個別3日間シナリオの役割

- 代表例
  - `AIカレッジFB広告_個別3日間シナリオ_1日目 (copy 02)`
  - `センサーズFB広告_個別3日間シナリオ_1通目ABテストA`
- 共通する意図
  - `あなた専用` という言葉で、自分ごと化を一段上げる
  - 3日間で urgency と social proof を積み、面談や作成会へ押し込む
  - 1日目はプレゼント、2日目は反響、3日目は締切で詰める
- current で見えている CTA
  - AI 側は `school.addness.co.jp/p/uUfErKGsQYZ2?...`
  - SNS 側は `school.addness.co.jp/page/KAtTy0tucas2?...`
- 本文で実際にやっていること
  - SNS 1通目 ABテストB は `【今だけ無料！】"あなた専用" SNS運用 1st副業スタートプログラム開催！！` と promise を先に立て、自分ごと化を最短で取りにいく
  - AI 側の current では `💬Google創業者に聞いた、僕らがGAFAを超える方法` のように、大きい夢や思想を使って `みかみ側の世界観に共感した人だけを濃くする` 役割を持つものもある
- 良いと判断できる点
  - `件名 -> 冒頭コピー -> CTA` が同じ圧力でつながっている
  - AB テストでは promise を変えつつ、構造は大きく崩していない

#### Mailchimp コンテンツ全体の理解

- コンテンツとは、`コミュニケーションの手段であり、ユーザーをこちらが意図した認識に変換するもの`
- Mailchimp で作るコンテンツは、その中でも `非リアルタイムの長文教育` に強い
- Mailchimp は `長文で教育しながら温度を上げる` 本線
- 件名は `物語 / 権威 / 危機感 / 緊急性 / 個別感` のどれかを強く担う
- 本文は長くてもよく、その代わり `1通ごとの役割` がかなり明確
- Lステップのような短い operational message ではなく、`教育と心理変容` が主目的
- current の良し悪しは、文章量ではなく `この1通で相手の心理をどこまで進めるか` が明確かどうかで判断する

#### 制作者意図を読む時の問い

Mailchimp の current コンテンツを読む時は、本文をうまく書いているかより先に、次を順に見る。

1. このメールは、直前の接点でどんな誤解や温度不足を受けているか
2. この 1 通で、どの認識を変えたいか
3. 変えた結果、次に何を押してほしいか
4. 件名、冒頭、本文、CTA がその 1 つの目的に揃っているか
5. 次の接点でも同じ文脈が続いているか

つまり、良い Mailchimp コンテンツは `文章が上手い` より、`変えたい認識が 1 通ごとに明確` である。

#### 良い / 悪いの判断をさらに具体化

- 良い
  - 件名の promise と本文冒頭の promise が一致している
  - 本文のストーリーや実例が、最後の CTA の説得材料になっている
  - CTA 先の short.io / UTAGE ページが、本文で作った期待とズレない
  - 1 通の役割が `教育` なのか `直オファー` なのか混ざっていない
- 悪い
  - 件名は強いが、本文の役割が散っている
  - 本文で温めた温度に対して、CTA が急すぎる
  - 本文は教育なのに、CTA だけが hard sell になっている
  - クリック先ページの約束が、メール本文の約束と変わる

#### シームレスな顧客体験として見たときの Mailchimp

- Mailchimp 単体で完結させるのではなく、前後の接点と滑らかにつながることが重要
- つまり 1 通ごとに見るべきなのは
  1. 直前の接点でユーザーが何を見たか
  2. このメールで何を理解させるか
  3. 次の接点で何をしてほしいか
- 良い current メールは
  - subject
  - 冒頭
  - 本文のストーリー
  - main CTA
  が同じ方向を向いている
- 悪いメールは
  - subject だけ強い
  - 本文の役割が曖昧
  - CTA が文脈とずれる
  というズレが起きる

#### current のコンテンツ判断で見る順番

1. これは `Journey` か `Campaign` か
2. 役割は `教育 / 直オファー / 横展開 / 締切ブースト / 単発promotion` のどれか
3. subject, 冒頭, CTA が同じ役割を向いているか
4. short.io や UTAGE の遷移先が、その役割に合っているか
5. 送信後は `送信 -> 開封 -> クリック -> click-details` の順で結果を見る

#### 直近 regular campaign の認識変換の読み方

- `AI全自動PR_1通目(3/13)`
  - subject: `【華金中に失礼します】　みかみから,プレゼントが届きました。`
  - main CTA: `direct LINE`
  - emails_sent: `259813`
  - open_rate: `8.57%`
  - click_rate: `0.187%`
  - 変えたい認識:
    - `AIはまだ自分ごとではない`
    - を
    - `いま受け取るべき具体メリットがある`
    - に変える
- `3/11 AIキャリアセミナーPR_1通目`
  - subject: `【🚨非IT職の方限定🚨】`
  - main CTA: `direct LIFF`
  - emails_sent: `257147`
  - open_rate: `9.58%`
  - click_rate: `0.045%`
  - 変えたい認識:
    - `自分向けか分からない`
    - を
    - `これは自分向けの urgent な案内だ`
    に変える
- `3/11 AIキャリアセミナーPR_2通目`
  - subject: `【非IT職の方へ朗報📣】`
  - main CTA: `direct LIFF`
  - emails_sent: `255250`
  - open_rate: `10.41%`
  - click_rate: `0.042%`
  - 変えたい認識:
    - `AI は IT 職向け`
    - を
    - `非IT職でも今のうちに動く価値がある`
    に変える
- `3/10 セミナーPR_1通目 (フリープラン)`
  - subject: `できる人ほど、気づくのが遅い。AI時代の損する働き方とは...`
  - main CTA: `direct LIFF`
  - emails_sent: `5012`
  - open_rate: `38.77%`
  - click_rate: `1.028%`
  - 変えたい認識:
    - `自分はまだ大丈夫`
    - を
    - `今の働き方のままだと損をする`
    に変える
- `3/10 セミナーPR_3通目 (フリープラン)`
  - subject: `日本は、10年以内に労働人口の『49％』が代替可能です。`
  - main CTA: `direct LIFF`
  - emails_sent: `5044`
  - open_rate: `38.87%`
  - click_rate: `0.654%`
  - 変えたい認識:
    - `まだ先の話だ`
    - を
    - `今すぐ考えるべき現実だ`
    に変える
- つまり current regular campaign は、`すぐ売る` というより
  - 危機感
  - 当事者意識
  - urgency
  を先に作って、次の予約や登録へ送る使い方が多い

#### regular campaign を作る時の exact チェック

- `Campaign`
  - 1 回きりの配信か
- `segment`
  - その温度に本当に合っているか
- `subject_line`
  - 認識を何に変えたいかが 1 行で分かるか
- 本文冒頭
  - 件名の promise をすぐ回収しているか
- `main CTA`
  - 原則 `short.io`
  - 例外で直リンクを使うなら理由が説明できるか
- `hyperlink`
  - 表示文字列ではなく、実 URL が意図どおりか
- click 後
  - `short.io -> UTAGE / LINE / 外部ページ`
  の最終遷移先までズレていないか

送る前に最低限やる順番:

1. `Journey` ではなく `Campaign` である理由を言えるか
2. `誰に送るか` を `saved segment / tag / exclude` で 1 回説明できるか
3. この 1 通の役割が
   - 危機感
   - authority
   - 予約促進
   - 締切
   のどれか 1 つに寄っているか
4. 本文内の main CTA が 1 本に絞られているか
5. 実 hyperlink を押して、最終遷移先まで確認したか

current の review で特に落としやすい点は次。

- `Segment` を切ったつもりで `Audience` 全体へ送る
- `Subject line` と本文冒頭の約束がズレる
- 本文上は short.io っぽく見えるが、実 hyperlink が別 URL のまま
- `short.io` は正しいが、その先の `UTAGE / LINE / 外部ページ` が古い
- 本文の main CTA と、最もクリックされている URL が一致していない

つまり regular campaign の quality は、

- 文章がうまいか

ではなく、

- その母集団に対して
- 何の認識を変え
- どの main CTA へ集約し
- クリック後の体験までズレなくつないでいるか

で判断する。

#### current campaign を 1 通ずつ読む helper

- 直近 sent regular campaign 一覧
  - `python3 System/scripts/mailchimp_campaign_snapshot.py --limit 12`
- 特定 campaign の詳細
  - `python3 System/scripts/mailchimp_campaign_snapshot.py --campaign-id e836af06b7`
- detail では
  - `subject_line`
  - `from_name`
  - `send_time`
  - `emails_sent`
  - `open_rate`
  - `click_rate`
  - `main_cta_type`
  - `main_cta_href`
  - `links`
  - `links_detailed`
  - `hyperlink_mismatches`
  - `text_preview`
  を 1 回で見られる
- `hyperlink_mismatches` に値が出た時は、表示文字列の URL と実 hyperlink がズレている候補として優先確認する

#### Journey と Campaign のコンテンツ差

- Journey
  - evergreen 前提
  - 連続接触で、認識を段階的に変える
  - 1 通単体より、前後の流れまで含めて設計する
- Campaign
  - 単発配信
  - 1 通で `今伝えるべきこと` を強く伝える
  - segment を切ったうえで、 urgency や topicality を前に出しやすい

つまり、同じ Mailchimp でも
- Journey は `流れの中で認識を育てる`
- Campaign は `その瞬間に必要な認識を一気に作る`
で役割が違う

### 実値で確認できたこと

- AI Facebook の `スキルプラスフリープラン1通目 (copy 03)` の本文には
  `school.addness.co.jp/p/TzYRwepfqzFq?...`
  が入っていた
- click report では
  `skill.addness.co.jp/ytad-ai2A`
  のような short.io が実際に押されていた

### URL 設置時の注意

- Mailchimp では、表示文字列と実際のハイパーリンク先がズレる事故が起こりうる
- そのため、本文内に URL を置いたら、配信前に必ず実際にクリックして確認する
- 確認順は次で固定する
  1. メール本文で CTA の表示文字列を確認する
  2. ハイパーリンクの実 URL を確認する
  3. 実際に押して、意図した short.io / UTAGE / 外部ページへ飛ぶか確認する
  4. short.io を使う場合は、short.io の最終遷移先まで確認する
- 特に `見えている URL` と `埋め込み URL` が違うケースを前提にする

### CLIコマンド（予定）

```bash
python3 cdp_mailchimp.py sync              # CDP → Mailchimpオーディエンス同期
python3 cdp_mailchimp.py sync --dry-run    # ドライラン
python3 cdp_mailchimp.py segment "条件"    # CDPセグメント → タグ付与
python3 cdp_mailchimp.py draft             # AIライティング → キャンペーン下書き
python3 cdp_mailchimp.py send {id}         # 承認済みキャンペーンを送信
python3 cdp_mailchimp.py report {id}       # 配信レポート取得
python3 cdp_mailchimp.py status            # 同期状態・未送信下書き一覧
```

---

## 着手時のステップ

### Step 1: API接続確認
- [x] 既存APIキーの動作確認
- [ ] `mailchimp-marketing` ライブラリのインストール
- [x] audience / list ID（`82aeac7431`）で Journey・step・content・report 取得テスト

### Step 2: CDP → Mailchimp同期
- [ ] CDPメールアドレスとMailchimpオーディエンスの突合
- [ ] 新規コンタクトのバッチ追加
- [ ] CDP属性（名前・流入経路等）のmerge fields同期

### Step 3: セグメント → タグ
- [ ] CDPフィルタエンジンの実装
- [ ] フィルタ結果 → Mailchimpタグ一括付与
- [ ] セグメント定義の保存・再利用

### Step 4: AIライティング → キャンペーン
- [ ] メールテンプレートの選定
- [ ] AIプロンプト設計（ブランドトーン・過去実績反映）
- [ ] キャンペーン下書き作成 → Slack承認通知

### Step 5: 送信 → レポート
- [ ] 承認後送信の実装
- [ ] 配信結果の自動取得・Slack通知
- [ ] Orchestrator連携（定期配信のスケジュール化）

---

## 注意事項

- **コンタクト上限超過中**: 現在347,826 / 300,000。CDP全員（56,538人）を追加しても既に超過しているため追加コスト影響は軽微。ただし重複チェックは必須
- **既存運用との共存**: 現在のスタッフによる手動配信フローを壊さない。`cdp_` タグで分離し、既存タグ・セグメントには触れない
- **addness.co.jpドメインは認証済み**: `@addness.co.jp` からの送信はそのまま可能
- **送信は取消不可**: `send` APIを叩いたら戻せない。承認フロー必須
- **Mailchimp側のデータ活用**: 34.8万コンタクトの開封・クリック履歴はCDPにない貴重なデータ。双方向同期で活用

---

## 関連ドキュメント

- [CDP](../4_AI基盤/CDP.md) — 顧客マスターデータの設計・同期
- [メール自動管理](./メール自動管理.md) — Gmail受信箱管理（本プロジェクトとは別系統）
