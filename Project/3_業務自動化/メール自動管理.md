# メール自動管理システム

## 基本情報

| 項目 | 内容 |
|------|------|
| プロジェクト名 | メール自動管理システム |
| 開始日 | 2026年2月18日 |
| 最終更新 | 2026年3月19日（`admin@snsers.com` 即レス基盤の入力シートと `onamae_mailbox_agent.py` を追加。IMAP / SMTP 接続成功、実メール往復テスト、常時監視用の安全ガード、運用方針の正本化を確認） |
| 完了日 | 2026年2月25日 |
| ステータス | ✅ 完了（継続改善中） |

## 概要

個人Gmail（koa800sea.nifs@gmail.com）の受信メールを自動的に取得し、AIで「返信必要」「返信不要」を分類。学習型の削除フローで、初回は確認→承認後は自動削除。5回以上削除した送信者は自動ブロック（メールが届かなくなる）。毎日8時に自動実行し、Slackに通知。

加えて、補助金前提の公式メール監視として `SECURITY ACTION` の `自己宣言ID` メールも個人Gmailから定期監視し、検知したら状態更新とLINE通知まで自動で行う。

### アーキテクチャ

```
┌─────────────┐                    ┌──────────────────┐
│   Gmail     │ ◀─── Gmail API ───│  mail_manager.py │
│   受信箱    │ ───────────────▶  │                  │
└─────────────┘                    │  - メール取得     │
                                   │  - AI分類        │
┌─────────────┐                    │  - 返信生成/送信  │
│   OpenAI    │ ◀── GPT-4o-mini ──│  - フィルター作成  │
│   API       │                    └──────────────────┘
└─────────────┘                            │
                                           │ Webhook
┌─────────────┐                            ▼
│   Slack     │ ◀──── 通知 ──────  #定常業務チャンネル
│   定常業務   │
└─────────────┘
```

## 実装した機能

### メール処理
1. **メール取得**: Gmail API で受信箱のメールを取得（ブロック済み送信者は除外、本文2000文字まで取得）
2. **AI分類**: OpenAI GPT-4o-mini で返信必要/不要を自動判定
3. **返信案生成**: 分類後、返信必要メールに対してIDENTITY.md（甲原文体）に基づく返信案を個別生成
4. **返信不要判定**: 領収書・自動通知等は「返信不要」理由を表示（無意味な返信案を出さない）
5. **承認ワークフロー**: Web UI で送信/保留/ゴミ箱/削除して学習/振り分けを選択
6. **自動送信**: 承認後、同一スレッド内で返信を自動送信
7. **不要メール削除**: 不要判定のメールをゴミ箱に移動

### 学習型削除フロー
8. **削除確認**: 初めての送信者は「削除確認待ち」に入る
9. **学習**: 「削除して学習」で送信者を「自動削除リスト」に追加
10. **ゴミ箱（学習なし）**: 今回だけゴミ箱に移動、送信者は学習しない（次回も通常受信）
11. **自動削除**: 学習済み送信者は確認なしで自動削除
12. **自動ブロック**: 5回以上削除でGmailフィルターを作成（以後メールが届かない）

### 保護機能
13. **ラベル保護**: フォルダ/ラベル分けされているメールは削除しない
14. **イベント保護**: イベント予約、Lステップ通知などは削除しない

### 通知
15. **Slack通知**: 処理結果を #定常業務 チャンネルに通知
16. **通知内容**: 返信待ち件数、削除確認待ち件数、自動削除件数
17. **公式メール監視通知**: `SECURITY ACTION` の自己宣言IDを検知したらLINE通知

### 保留機能
18. **保留記憶**: 保留にしたメールのIDを `state.json` の `held_message_ids` に記録
19. **再表示防止**: 保留済みメールは次回以降の分類処理でスキップされ、Web UIに再表示されない
20. **削除確認の保留**: 削除確認画面の「保留」ボタンも同じ動作（受信トレイに残しつつ非表示）

### 補助金前提メール監視
21. **SECURITY ACTION監視**: 個人Gmailから `SECURITY ACTION / 自己宣言ID` メールを定期検索
22. **自己宣言ID抽出**: `4で始まる11桁` を本文・件名から抽出
23. **状態更新**: 補助金メモの監視ステータス欄を更新
24. **LINE通知**: 初回検知時のみ「前提が揃った」通知を送る

### フォルダ振り分け（Web UI）
25. **ラベル選択**: 既存のGmailラベル一覧から選択
26. **ラベル作成**: 新規ラベルをその場で作成可能
27. **フィルター作成**: 送信者→ラベルの自動振り分けフィルターを作成（今後の受信に自動適用）
28. **即時振り分け**: フィルター作成時、対象メッセージにもラベル付与＋受信トレイから即除去
29. **受信トレイスキップ**: メールを直接フォルダに入れる設定

## 技術スタック

| 項目 | 技術 |
|------|------|
| 言語 | Python 3.10+（Mac Mini 常駐環境は `agent-env` の Python 3.12 を優先） |
| メールAPI | Gmail API (OAuth 2.0) |
| AI | OpenAI GPT-4o-mini |
| 通知 | Slack Incoming Webhooks |
| スケジューラ | cron (毎時0分) |
| Web UI | Flask (localhost:5001) |

## 関連ファイル

| パス | 説明 |
|------|------|
| `System/mail_manager.py` | メインスクリプト |
| `System/onamae_mailbox_agent.py` | お名前.com 系メールボックスの即レス基盤ひな型（IMAP / SMTP / 未読取得 / 返信案生成 / 明示送信） |
| `System/security_action_monitor.py` | `SECURITY ACTION` 自己宣言IDメールの定期監視・抽出 |
| `System/receipt_downloader.py` | 領収書メールの添付・本文を取得して `Master/output/経理/領収書/年/月` に保存 |
| `Master/output/経理/領収書/` | 領収書保存フォルダ（年/月で整理）。README に rclone アップロード例あり |
| `System/mail_review_web/app.py` | Web UI（Flask） |
| `System/mail_inbox_data/state.json` | 送信者カウント・ブロック・学習・保留リスト |
| `System/mail_inbox_data/pending.json` | 返信待ちメール |
| `System/mail_inbox_data/delete_review.json` | 削除確認待ちメール |
| `System/mail_inbox_data/config.json` | Slack設定・OpenAI APIキー |
| `System/credentials/client_secret_personal.json` | 個人Gmail / 外部 Gmail 用 OAuth 認証情報 |
| `System/credentials/client_secret.json` | 会社 Google アカウント用 OAuth 認証情報 |
| `System/credentials/token_gmail_personal.json` | 個人Gmail用トークン |
| `System/credentials/token_gmail_adteam.json` | addness.adteam@gmail.com 用トークン |
| `System/credentials/snsers_admin_mailbox.json` | `admin@snsers.com` 即レス基盤の入力シート（IMAP / SMTP / 返信ポリシー） |
| `System/data/snsers_admin_mailbox_state.json` | `admin@snsers.com` 即レス基盤の処理済み IMAP ID と最終処理時刻 |
| `System/data/snsers_admin_mailbox_latest.json` | 直近の未読取得 / 返信案生成 / run-once 結果のスナップショット |
| `System/mail_manager.log` | 実行ログ |

## 認証情報

| 項目 | 値 |
|------|------|
| Gmailアカウント | koa800sea.nifs@gmail.com / addness.adteam@gmail.com / kohara.kaito@team.addness.co.jp |
| トークンファイル | `System/credentials/token_gmail_*.json` |
| OAuth認証情報 | `System/credentials/client_secret_personal.json`（個人Gmail / 外部 Gmail）, `System/credentials/client_secret.json`（会社 Google アカウント） |
| Slackアプリ名 | メール通知 |
| Slackチャンネル | #定常業務 |
| Webhook URL | config.json に保存済み |
| OpenAI APIキー | config.json に保存済み |

## 使い方

### 自動処理の流れ

```
毎時0分: 自動実行（cron）
    ↓
新着メールを取得・AI分類
    ↓
学習済み送信者は自動削除
    ↓
返信待ち・削除確認待ちがあればSlackに通知
    ↓
Web UI または Cursor で確認・処理
```

### Web UI（推奨）

```
http://localhost:5001/?account=personal
```

- 返信待ち・削除確認をブラウザで一覧表示
- 返信待ち: 「送信」「保留」「ゴミ箱」「削除して学習」「📁 振り分け」ボタン
- 削除確認: 「ゴミ箱」「削除して学習」「保留」「📁 振り分け」ボタン
- 保留したメールは受信トレイに残るが、今後Web UIには表示されない
- 「📁 振り分け」でフォルダ振り分け設定（既存ラベル選択 or 新規作成）
  - フィルター作成と同時に、現在のメールも即座に移動される

### 基本コマンド

```bash
# メール取得→分類→通知
python3 System/mail_manager.py run

# Web UI 起動
python3 System/mail_review_web/app.py

# 状態確認
python3 System/mail_manager.py status

# Slack通知テスト
python3 System/mail_manager.py slack-test

# 領収書メールから添付・本文をダウンロード（個人Gmail）
python3 System/receipt_downloader.py "Payment receipt" --from creem

# admin@snsers.com 接続確認
python3 System/onamae_mailbox_agent.py test-connection

# admin@snsers.com 未読確認
python3 System/onamae_mailbox_agent.py list-unseen --limit 5

# admin@snsers.com 最新未読の返信案生成（送信なし）
python3 System/onamae_mailbox_agent.py draft-latest --limit 1

# admin@snsers.com run-once（送信なし）
python3 System/onamae_mailbox_agent.py run-once

# admin@snsers.com run-once（実送信）
python3 System/onamae_mailbox_agent.py run-once --send

# admin@snsers.com 常時監視（auto_send_enabled=true の時だけ有効）
python3 System/onamae_mailbox_agent.py monitor --interval 15
```

### 自動実行（設定済み）

```
毎時0分に自動実行（cron）
- 個人: System/mail_inbox_personal.log
- 会社: System/mail_inbox_kohara.log
- SECURITY ACTION監視: 3時間ごと 15分
```

## AI分類の基準

| 判定 | 条件 |
|------|------|
| 返信必要（削除しない） | イベント予約、Lステップ通知、個人宛メッセージ、セキュリティ警告 |
| 返信不要（削除候補） | ニュースレター、広告、SNS通知、サービス更新通知 |
| 保護 | ラベル/フォルダ分けされているメールは削除しない |

## 学習フロー

| 回数 | 動作 |
|------|------|
| 1回目 | 削除確認 → 承認で学習 |
| 2〜4回目 | 自動削除（確認なし） |
| 5回目 | 自動削除 → ブロック（メールが届かなくなる） |

## state.json の構造

| キー | 型 | 説明 |
|------|------|------|
| `not_needed_count` | `{sender: count}` | 送信者ごとの不要判定回数 |
| `blocked_senders` | `string[]` | ブロック済み送信者 |
| `auto_delete_senders` | `string[]` | 自動削除対象の送信者 |
| `held_message_ids` | `string[]` | 保留済みメッセージID（今後非表示） |

## 注意事項

- Gmail OAuth トークンは定期的に更新が必要。`invalid_grant` や revoke で refresh に失敗した場合は、壊れた token を `*.invalid.*.json` へ退避する
- cron などの非対話実行では browser reauth を始めず、Slack に「再認証が必要」と通知して即終了する。再認証は MacBook の対話ターミナルで `python3 System/mail_manager.py --account {account} run` を実行する
- 対話ターミナルで実行した場合は browser reauth に自動で戻る
- OpenAI API の利用料金に注意
- ブロックフィルターは「アーカイブ」なので、完全削除ではない
- Macがスリープ中はcronが実行されない
- `held_message_ids` はメッセージIDで管理しているため、同一送信者の別メールは保留にならない

## admin@snsers.com 即レス基盤（ひな型）

- 認証ファイルは `System/credentials/snsers_admin_mailbox.json`
- 2026-03-19 時点で `webmail74.onamae.ne.jp`
  - IMAP `993`
  - SMTP `465 (SSL)` / `587 (STARTTLS)`
  の接続成功を確認
- 送信箱ヘッダを確認した結果、既存の差出人表示名は `みかみ`
- 返信人格の正本は `Master/self_clone/mikami/IDENTITY.md`
- `test-connection` で IMAP mailbox 一覧取得と SMTP `NOOP` まで確認可能
- `draft-latest` は未読メールを取得し、同一送信者の直近文脈を見て返信要否を判定したうえで返信案を生成する
- `run-once` は送信フラグなしだと送らず、`--send` を付けた時だけ SMTP 返信する
- `monitor` は `reply_profile.auto_send_enabled = true` の時だけ開始する。false のままでは勝手に自動送信しない
- 返信不要判定の初期ルールとして、`配信解除` を含む広告・一斉配信は自動返信しない
- 自動送信の安全ガードとして、`返金 / 解約 / クレーム / 訴訟 / 弁護士 / 消費者センター / 詐欺 / 個人情報 / 支払ったのに` を含むメール、添付付きメール、即レス期限を超えた未読は `needs_review` で止める
- `koa800sea.nifs@gmail.com` からのテストメール `ZZ_TEST_ONAMAE_20260319_1` に対して、`admin@snsers.com` から `Re:` 返信が実着弾するところまで確認済み
- 直近スナップショットは `System/data/snsers_admin_mailbox_latest.json` に保存される

### 2026-03-19 時点の運用方針

- 現状、`admin@snsers.com` の返信は止まっておらず、送信箱には直近14日で 235 通の送信がある。既存コード上にこの mailbox 専用の自動返信基盤は見当たらないため、主運用は手動返信の可能性が高い
- したがって、いきなり全面自動送信には入らず、まずは `秘書グループ承認 -> 送信` を先に作る
- 承認先 LINE は `秘書グループ`
- 将来ゴールは `full_auto_send`。ただし段階的に寄せる
- 基本の返信人格は `三上功太`、差出人表示は `みかみ`
- `返金 / 解約 / 領収書 / 決済エラー / 契約 / 法務` は `アドネス株式会社` 側の事務連絡として分離する
- メルマガ感想への返信ゴールは `関係性を深める / 次のメルマガを読んでもらう / 不足教育を補う`
- 購入検討の質問への返信ゴールは `不安を解消する`。個別相談が必要なら個別相談へ進める
- `購入しました` の報告には、ここから前向きに進める温度感を返す
- CTA の原則は `そのユーザーにとって今何をすべきかを提示する`
- 教育設計の原則は `何の教育が足りずに個別相談に来ていないのかを逆算し、不足教育を与える`
- 次のメルマガで教育できるなら次を促し、YouTube の方が早いなら動画リンクを返す
- このタスクはここで一旦 `保留`。再開時は、`秘書グループ承認フロー接続 -> CDP参照 -> メール種別ごとの返信目的分岐` の順で進める

## 更新履歴

| 日付 | 内容 |
|------|------|
| 2026-02-18 | 初版作成。メール取得・分類・返信・ブロック・Slack通知 |
| 2026-02-18 | 個人Gmail対応、学習型削除フロー、ラベル保護、5回ブロック、毎日8時自動実行 |
| 2026-02-19 | Web UIにフォルダ振り分け機能を追加（ラベル選択・新規作成・フィルター設定） |
| 2026-02-21 | 保留機能強化（held_message_idsで再表示防止）、振り分け時の即時移動、削除確認「残す」→「保留」に統一 |
| 2026-02-24 | 領収書ダウンロード（receipt_downloader.py）、領収書保存フローを追加 |
| 2026-03-08 | 保存先を OS に合わせて `Master/output/経理/領収書/` に移動 |
| 2026-02-25 | 返信案生成を大幅改善（IDENTITY.md文体反映・分類と分離・本文2000文字対応・返信不要判定）、「ゴミ箱」ボタン追加（学習なし削除） |
| 2026-03-11 | `SECURITY ACTION` 自己宣言IDメールの自動監視・抽出・LINE通知を追加 |
| 2026-03-13 | `invalid_grant` 時の token 自動退避、非対話実行での再認証待ち防止、Slack での再認証案内を追加 |
| 2026-03-19 | `System/credentials/snsers_admin_mailbox.json` を追加。`admin@snsers.com` の即レス返信基盤に必要な入力を1ファイルへ集約 |
| 2026-03-19 | `System/onamae_mailbox_agent.py` を追加。`admin@snsers.com` の IMAP / SMTP 接続確認、未読取得、返信要否判定、返信案生成、明示送信のひな型を実装 |
| 2026-03-19 | `admin@snsers.com` で実メール往復テストを実施。`koa800sea.nifs@gmail.com` からのテストメール受信、返信案生成、SMTP返信、Gmail側での着弾確認まで完了 |
| 2026-03-19 | `onamae_mailbox_agent.py` に常時監視 `monitor` と自動送信の安全ガードを追加。危険キーワード・添付付き・古い未読は自動送信せず `needs_review` で停止するよう更新 |
| 2026-03-19 | `reply_profile` に `from_name=みかみ` / `persona_name=三上功太` / `identity_path=Master/self_clone/mikami/IDENTITY.md` を設定。既存送信箱ヘッダの表示名 `みかみ` と一致させた |
| 2026-03-19 | 承認先を `秘書グループ` に固定。`三上人格` と `アドネス株式会社` 連絡の分離条件、メルマガ感想・購入検討・購入報告の返信ゴール、将来の全自動化方針を文書と設定に保存し、このタスクを一旦保留にした |
