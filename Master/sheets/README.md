# 管理シート参照

最終更新: 2026-03-18

## 情報ラベル

- 所有元: internal
- 開示レベル: role-limited
- 承認必須: conditional
- 共有先: 僕 / 上司 / 並列

外部の管理シート（Google Sheets等）への参照と、同期データを格納する。

## 情報ラベルの既定値

- `Master/sheets/README.md`
  - 所有元: `internal`
  - 開示レベル: `role-limited`
  - 承認必須: `conditional`
  - 共有先: `僕 / 上司 / 並列`
- `Master/sheets/*/_meta.json`
  - 所有元: `internal`
  - 開示レベル: `role-limited`
  - 承認必須: `conditional`
  - 共有先: `僕 / 上司 / 並列`

シートの `_meta.json` は同期キャッシュのため構造を変えず、path 既定値で監査する。

## 登録シート一覧

| ID | シート名 | URL | 用途 | 同期 |
|---|---|---|---|---|
| `1FOh_XGZWaEisfFEngiN848kSm2E6HotAZiMDTmO7BNA` | 【アドネス全体】数値管理シート | [リンク](https://docs.google.com/spreadsheets/d/1FOh_XGZWaEisfFEngiN848kSm2E6HotAZiMDTmO7BNA/edit) | 事業KPI・会社全体の重要数値。集客数・売上・会員数等を知りたいときに参照 | API経由で都度取得 |
| `1w-eFCzbGjgmoiuZe54X49BVZw46vm7hg6_CXI7f_gmw` | 【アドネス株式会社】KPI基盤（正本） | [リンク](https://docs.google.com/spreadsheets/d/1w-eFCzbGjgmoiuZe54X49BVZw46vm7hg6_CXI7f_gmw/edit) | 5KPIの一次データ正本。raw / fact / view を分けて、経営会議・日報・Cursor の共通入力元にする | ✅ 03/13 22:02 (32603行) |
| `16W1zALKZrnGeesjTlmsraDfw3i71tcdYJE686cmUaTk` | 【広告チーム】報告シート | [リンク](https://docs.google.com/spreadsheets/d/16W1zALKZrnGeesjTlmsraDfw3i71tcdYJE686cmUaTk/edit) | チーム報告シート（日報・週報・月報・年報）。目標と現状のギャップ把握→アクション導出用。**毎日使用** | API経由で都度取得 |
| `1qjU279OVD0i4h2AdQzkYIsZCfA1BeiUKLHNg7i2a2fk` | 【アドネス株式会社】顧客データ_複数イベント（統合） | [リンク](https://docs.google.com/spreadsheets/d/1qjU279OVD0i4h2AdQzkYIsZCfA1BeiUKLHNg7i2a2fk/edit) | CDP顧客マスタ。イベント発生者（セミナー予約・購入等）の統合データ | API経由で都度取得 |
| `1iD3DGxNhZruyjYcA5n6oXRDk2ZGA3uMDOo0stQS9Y00` | 【アドネス株式会社】顧客データ_メールアドレスのみ（統合） | [リンク](https://docs.google.com/spreadsheets/d/1iD3DGxNhZruyjYcA5n6oXRDk2ZGA3uMDOo0stQS9Y00/edit) | リードプール。メールアドレスのみのリード（イベント未発生）。`データソース管理` タブは `cdp_sync.py` が `正常 / 未同期 / 停止` と `最終同期日 / 更新数` を自動反映し、full sync の最後に `顧客マスタ` と重なるメールを毎回掃除する | API経由で都度取得 |
| `1dSIXBovs-c8wVnBWsOqbe2wdqmJQ10bOIWhKJbC1MPw` | 【アドネス株式会社】共通除外マスタ | [リンク](https://docs.google.com/spreadsheets/d/1dSIXBovs-c8wVnBWsOqbe2wdqmJQ10bOIWhKJbC1MPw/edit) | 共通除外の正本。`除外リスト / 無条件除外ルール / データソース管理 / データ追加ルール` を持ち、集客・個別予約・決済などの新規データだけを共通除外する。候補元は `支払管理表 / 全メンバーリスト`、正本はこのシートの `除外リスト`。`test / テスト / sample / dummy` など明らかなテストデータは `無条件除外ルール` を機械参照して除外する | `python3 System/common_exclusion_master_setup.py` で再生成 |
| `1kxUbLqhnzLC1Pg0ASVgU135bnx4Rsv_jP0pqGC0R69w` | マスタデータ | [リンク](https://docs.google.com/spreadsheets/d/1kxUbLqhnzLC1Pg0ASVgU135bnx4Rsv_jP0pqGC0R69w/edit) | 共通マスタの入力口。`定義一覧 / 流入経路マスタ / 商品マスタ` を持ち、KPI用語や各マスタを人が整理する場所。`定義一覧` は `【アドネス株式会社】KPIダッシュボード / 日別数値` で使う項目定義の人向け正本として扱う | `python3 System/kpi_dashboard_layout_setup.py` で `定義一覧` を同期 |
| `13HS9KmlTdxQwMMaK45H3Ga1mMTUiJdhYKWnrExge_yY` | 【アドネス株式会社】集客データ_メール集計（加工） | [リンク](https://docs.google.com/spreadsheets/d/13HS9KmlTdxQwMMaK45H3Ga1mMTUiJdhYKWnrExge_yY/edit) | 集客メールの統合加工シート。`日別メール登録件数 / 日別メール登録件数（UU） / 複数アドレスユーザー / メール集計サマリー / データソース管理 / データ追加ルール` を一箇所にまとめる。`2025/01/01 - 2026/03/06` はローカル固定スナップショット、`2026/03/07` 以降は `【アドネス株式会社】集客データ_メールアドレス（収集）` を正にする。`メール登録ユーザー数` は `メール登録件数（UU） - 複数アドレスユーザー数` で持つ | `python3 System/email_collection_metrics_sheet_sync.py` で再生成 |
| `12bYadR0cgi24t4tz8GeESlsKffmNkkTHprI4ray_Sq4` | 【アドネス株式会社】個別面談データ（収集） | [リンク](https://docs.google.com/spreadsheets/d/12bYadR0cgi24t4tz8GeESlsKffmNkkTHprI4ray_Sq4/edit) | 個別予約の収集データシート。`個別予約通知ログ / データソース管理 / データ追加ルール` を持ち、`Slack #個別予約通知` を継続取得の正本として `1イベント1行` で保存する。列は `予約イベント日時 / LINE名 / LSTEPメンバーID / Lステップアカウント名 / Lステップリンク / メールアドレス / 電話番号`。過去データは初回移行時のみ統合し、継続取得の正本にはしない。取りこぼし防止のため、毎回直近数日の Slack 通知を再取得して重複吸収する。`データソース管理` では `Slack 取得` と `Lステップ live 補完` を別行で管理する | `python3 System/booking_notification_log_sync.py` で更新 |
| `1ip_RARDHmQvTjmaVavw1L71ltPrn4Kg6sa__njqyQZ8` | 【アドネス株式会社】個別面談データ（加工） | [リンク](https://docs.google.com/spreadsheets/d/1ip_RARDHmQvTjmaVavw1L71ltPrn4Kg6sa__njqyQZ8/edit) | 個別予約の第1版加工シート。`【アドネス株式会社】個別面談データ（収集） / 個別予約通知ログ` を正本にして `2025/01/01` から `日別個別予約数` を作る。`日別個別予約数 / 日別個別予約数（UU） / 個別予約サマリー / データソース管理 / データ追加ルール` を持つ。現時点で接続するのは `個別予約数` のみで、`個別予約数（UU）` と `個別実施数` は将来接続 | `python3 System/booking_metrics_sheet_sync.py` で更新 |
| `1VwAO5rxib8pcR7KgGn-T3HKP7FaHqZmUhIBddo3okyw` | 【アドネス株式会社】会員データ（収集） | [リンク](https://docs.google.com/spreadsheets/d/1VwAO5rxib8pcR7KgGn-T3HKP7FaHqZmUhIBddo3okyw/edit) | 会員系イベントの収集シート。`会員イベント / データソース管理 / データ追加ルール` を持ち、`1行 = 1契約単位` で `メールアドレス / 電話番号 / 名前 / LINE名 / 契約締結日 / 入金前契約解除 / クーリングオフ / 中途解約` を保持する。`全面談合算` と `お客様相談窓口_進捗管理シート` の事実だけを集約し、加工ロジックは持たない。`会員イベント` には `契約締結日` がある契約だけを保持し、`なし` や `メアドなし` などメールアドレスではない値は保存しない。`データソース管理` は列単位で取得元と優先度を管理し、例として `メールアドレス = 全面談合算 -> クーオフ -> 中途解約`、`名前 = 全面談合算の名前 -> 各窓口シートの本名`、`LINE名 = 全面談合算のLINE名 -> 各窓口シートのLINE名` の順で補完する。`更新数` はソース総件数ではなく、その列へ実際に反映された件数を示す。`正常 / 未同期 / 停止`、最終同期日、更新数、エラー数を持ち、2時間ごとに自動更新する | `python3 System/membership_collection_sheet_setup.py` で更新 |
| `1OFKvyQsydPmTqd9MwSMX53MXxG9ASfkFquyf4PV-M8E` | 【アドネス株式会社】会員データ（加工） | [リンク](https://docs.google.com/spreadsheets/d/1OFKvyQsydPmTqd9MwSMX53MXxG9ASfkFquyf4PV-M8E/edit) | 会員系KPIの加工シート。`日別会員数値 / 会員サマリー / データソース管理 / データ追加ルール` を持ち、`2025/01/01` 以降の `契約数 / クーリングオフ数 / 会員数 / 中途解約数 / アクティブ会員` を日次に落とす。`会員数` は `契約締結日 + 7日` を会員化日とするが、`今日以前に会員化が確定した件数` だけを保持する。`入金前契約解除` は日別には持たず、会員化除外とサマリー確認だけに使う | `python3 System/membership_metrics_sheet_sync.py` で更新 |
| `1utCt9ex0puEi3-oxcjq9v37Jt-9X_dpSjPowZBsHeqA` | 【アドネス株式会社】KPIダッシュボード | [リンク](https://docs.google.com/spreadsheets/d/1utCt9ex0puEi3-oxcjq9v37Jt-9X_dpSjPowZBsHeqA/edit) | 事業全体の表示層。第1版は `スキルプラス事業サマリー / 日別数値 / データソース管理` の3タブだけを表に出す。項目定義は `マスタデータ / 定義一覧` に集約し、このシートでは持たない。`スキルプラス事業サマリー` は `日別数値` を期間指定で集計し、右側に `集客 / 予約・実施 / 売上・広告費 / 会員` の4グラフを置く。`日別数値` は `日付 / 集客数 / 集客数（UU） / 個別予約数 / 個別予約数（UU） / 個別実施数 / 着金売上 / 広告費 / CPA / 個別予約CPO / ROAS / 会員数 / 中途解約数 / クーリングオフ数` の14列を正本の受け口にする。現時点では `集客数 / 集客数（UU） / 個別予約数 / 会員数 / 中途解約数 / クーリングオフ数` まで接続済みで、`スキルプラス事業サマリー / 日別数値 / データソース管理` は自動生成タブとして保護済み。`データソース管理` では各項目の `正常 / 未同期 / 停止`、最終同期日、更新数、エラー数を確認できる | `python3 System/kpi_dashboard_layout_setup.py` で骨格更新 |
| `1mtfvXN92_vtzwLhOiTcufdLJ6vkfn8oYjCiqC0ZK6j8` | 【アドネス株式会社】集客データ_UUメールアドレス（加工） | [リンク](https://docs.google.com/spreadsheets/d/1mtfvXN92_vtzwLhOiTcufdLJ6vkfn8oYjCiqC0ZK6j8/edit) | 旧加工シート。`顧客マスター + メールのみ` を母集団にした `UUメールアドレス / 日別UUメールアドレス数 / 複数アドレスユーザー / メール集計サマリー / データソース管理 / データ追加ルール` を持つ。現在の主系統は `【アドネス株式会社】集客データ_メール集計（加工）` であり、このシートは legacy として保持する | 定期実行停止。必要時のみ手動再生成 |
| `1RsRkGaHCFsFc1nT1lMQFyNG-f13llH4UX9Bl_EYxNjU` | 【アドネス株式会社】集客データ_全メール登録数（加工） | [リンク](https://docs.google.com/spreadsheets/d/1RsRkGaHCFsFc1nT1lMQFyNG-f13llH4UX9Bl_EYxNjU/edit) | 旧加工シート。`日別集客数シート / 2025年2月CSV / 集客データ_メールアドレス（収集）` を役割分担して使う `日別メール登録件数 / メール登録件数サマリー / データソース管理 / データ追加ルール` を持つ。現在の主系統は `【アドネス株式会社】集客データ_メール集計（加工）` であり、このシートは legacy として保持する | 定期実行停止。必要時のみ手動再生成 |
| `12RGMUfU8Wj0CCdcRfY7kI56kdATV7wDXjvYmGdQb_Nk` | 【スキルプラス】個別予約AIテレアポ管理 | [リンク](https://docs.google.com/spreadsheets/d/12RGMUfU8Wj0CCdcRfY7kI56kdATV7wDXjvYmGdQb_Nk/edit) | スキルプラス販売導線における AIテレアポ運用の管理シート。顧客マスタを正本に、架電一覧・架電履歴・架電成績・運用状態を管理する | API経由で都度取得 |
| `1nkmeWcHmzxPJH1d5veXwTpDoZrsPFH4_3txevqj225Y` | 言葉の定義 | [リンク](https://docs.google.com/spreadsheets/d/1nkmeWcHmzxPJH1d5veXwTpDoZrsPFH4_3txevqj225Y/edit) | 甲原さんが使う言葉の定義集。入力口はシート、AI参照正本は `Master/前提/用語定義.md` / `.json` | ✅ 03/12 00:25 (128行) |

## ディレクトリ構成

```
sheets/
├── README.md           ← このファイル（シート一覧・用途・取得コマンド）
└── {シートID}/
    ├── _meta.json      ← シートのメタ情報（タブ名・行数・列数）
    └── {tab名}.csv     ← タブごとの同期キャッシュ
```

> **API取得が正本、CSVは高速参照用キャッシュ。**
> 人が厳密に確認するときは必要に応じて API / 元シートを見直し、AI の高速参照や差分確認には `sheets_sync.py` が生成するローカルキャッシュを使う。

## シート詳細

### 【アドネス全体】数値管理シート

事業のKPIや会社全体の数値を確認するためのマスターシート。

| タブ名 | 内容 |
|---|---|
| 元データ | 997行×26列。集計元の生データ |
| スキルプラス（日別） | 6036行×13列。日別の詳細数値 |
| スキルプラス（月別） | 100行×12列。月別サマリー |

```bash
# データ取得コマンド
python3 System/sheets_manager.py read "1FOh_XGZWaEisfFEngiN848kSm2E6HotAZiMDTmO7BNA"
python3 System/sheets_manager.py read "1FOh_XGZWaEisfFEngiN848kSm2E6HotAZiMDTmO7BNA" "スキルプラス（月別）"
```

### 【アドネス株式会社】KPI基盤（正本）

Looker や日報を正本にせず、一次データから 5KPI を再現するための基盤シート。

| タブ名 | 内容 |
|---|---|
| README | 5KPI定義、レイヤー構造、次の投入順 |
| raw_ads_* | Meta / Google / TikTok / X の広告生データ入口 |
| raw_funnel_events | lead / booking / attend 等のイベント入口 |
| raw_payments | 決済・返金の入口 |
| raw_membership | 入会 / クーリングオフ / 中途解約の入口 |
| route_master | `route_key = 媒体|ファネル|CR|LP` の定義 |
| 導線データ | 1イベント1行の正本 |
| 媒体×CR×LPデータ | `1日 × 1 route_key × 1行` の正本 |
| 顧客マスタ | 1人1行の正本 |
| kpi_daily | 日別再集計ビュー |
| exec_dashboard | 経営会議・日報・Cursor 向けの表示タブ |

```bash
# 初期構築
python3 System/kpi_foundation_setup.py

# 決済履歴の反映
python3 System/kpi_foundation_payment_import.py

# シート確認
python3 System/sheets_manager.py info "1w-eFCzbGjgmoiuZe54X49BVZw46vm7hg6_CXI7f_gmw"
```

### 【広告チーム】報告シート

目的や目標に対しての現状を把握し、ギャップを埋めるためのアクションを出しやすくすることで、ビジネスモデルの強化・維持を図るためのシート。毎日使用。

| タブ名 | 内容 |
|---|---|
| 目的・ルール | シートの使い方・ルール定義 |
| 日報 | 1047行×261列。日次の数値報告（集客数・個別予約数・着金売上等） |
| 週報 | 週次サマリー |
| 月報 | 月次サマリー |
| 年報 | 年次サマリー |
| 表・グラフ | 可視化用 |

```bash
# データ取得コマンド
python3 System/sheets_manager.py read "16W1zALKZrnGeesjTlmsraDfw3i71tcdYJE686cmUaTk" "日報"
python3 System/sheets_manager.py read "16W1zALKZrnGeesjTlmsraDfw3i71tcdYJE686cmUaTk" "週報"
```

---

### 【アドネス株式会社】顧客データ_複数イベント（統合）＝ CDPマスタ

イベント発生者（セミナー予約・購入・アンケート回答等）の顧客データを統合管理するマスターシート。

| タブ名 | 内容 |
|---|---|
| 顧客マスタ | 約57,750行×58列。全顧客の統合データ |
| データソース管理 | カラムマッピング（CDPカラム→参照先） |
| マスター追加ルール | 登録・名寄せ・更新のルール |
| 除外リスト | スタッフ・テストデータの除外対象 |

```bash
# 同期コマンド（cdp_sync.py経由）
python3 System/cdp_sync.py sync
python3 System/cdp_sync.py status
```

### 【アドネス株式会社】顧客データ_メールアドレスのみ（統合）＝ リードプール

CDPマスタに未登録のリード（メールアドレスのみ・イベント未発生）を格納。

| タブ名 | 内容 |
|---|---|
| メール集客データ | 約278,000行×3列。登録日/メールアドレス/初回流入経路 |
| データソース管理 | ソースマッピング |
| データ追加ルール | 追加条件・削除ルール |

```bash
# 同期コマンド（経路別タブ→メール一覧）
python3 System/cdp_sync.py sync-leads
```

### 【アドネス株式会社】集客データ_UUメールアドレス（加工）

メール登録の重複なし人数を持つための専用シート。未転換リード一覧とは役割を分ける。

| タブ名 | 内容 |
|---|---|
| UUメールアドレス | `登録日 / メールアドレス` の2列。母集団は `顧客マスター + メールのみ`。登録日の正本は `集客データ（メールアドレス）` の各流入経路タブ A列で、同じメールは最も古い日付を使う |
| 日別UUメールアドレス数 | `日付 / UUメールアドレス数 / 累計UUメールアドレス数`。初回登録日ベースの日次集計 |
| 複数アドレスユーザー | `顧客ID / メールアドレス数 / メールアドレス`。1人で複数メールを持つ顧客を確認する |
| メール集計サマリー | `更新日時 / UUメールアドレス数 / 登録日ありUUメールアドレス数 / 登録日空欄UUメールアドレス数 / 複数アドレスユーザー数 / 主メール重複数 / 実際のユニーク人数（暫定）` |
| データソース管理 | 母集団に使うシートと、登録日補完だけに使うシートを固定する |
| データ追加ルール | 母集団、登録日、手編集禁止、異常検知、更新頻度の運用ルール |

```bash
# 6タブまとめて再生成
python3 System/unique_email_sheet_sync.py

# 主メール重複の統合
python3 System/cdp_sync.py merge-primary-email-duplicates
```

- 定期更新: Orchestrator が `18 */2 * * *` で2時間ごとに再生成
- 異常検知: `主メール重複 > 0` または `UU件数の急減` を検知した場合は更新を止める
- 保護: `UUメールアドレス / 日別UUメールアドレス数 / 複数アドレスユーザー / メール集計サマリー` は保護対象

---

### 【アドネス株式会社】集客データ_全メール登録数（加工）

メールアドレスの `登録件数` を、`登録という事実` として重複ありで持つための専用シート。

| タブ名 | 内容 |
|---|---|
| 日別メール登録件数 | `日付 / メール登録件数 / 累計メール登録件数`。`2025/01/01 - 2026/03/06` は `【アドネス】顧客管理シート / 日別集客数` の `広告` セクションと `2025年2月CSV` を使い、`2026/03/07` 以降は `集客データ（メールアドレス）` を使って重複あり集計 |
| メール登録件数サマリー | `更新日時 / 全メール登録件数 / 日別集計日数 / 最初の登録日 / 最新の登録日 / 現行参照元タブ数 / 過去件数ソース日数 / 2025年2月補完ファイル数 / 集計対象外行数` |
| データソース管理 | `日別集客数シート / 2025年2月CSV / 集客データ（メールアドレス）` の役割分担と、廃止した旧補完を固定する |
| データ追加ルール | `1登録=1件`、`2024/06/11 起点の旧補完は使わない`、`2026/03/07 以降は現行元データを正とする` などの運用ルール |

```bash
# 4タブまとめて再生成
python3 System/email_registration_count_sheet_sync.py
```

- 定期更新: Orchestrator が `28 */2 * * *` で2時間ごとに再生成
- 異常検知: `全メール登録件数の急減` や `参照元タブ0件` を検知した場合は更新を止める
- 保護: 4タブとも自動生成タブとして保護対象

---

### 【アドネス株式会社】集客データ_メール集計（加工）

メール集客の `重複あり件数` と `集客UU` を一箇所にまとめた統合シート。今後の集客運用で見る主シート。

| タブ名 | 内容 |
|---|---|
| 日別メール登録件数 | `日付 / メール登録件数 / 累計メール登録件数`。`2025/01/01 - 2026/03/06` は `Master/output/email_collection_metrics_history_snapshot.json` の固定値を継承し、`2026/03/07` 以降は `【アドネス株式会社】集客データ_メールアドレス（収集）` を正本にして重複ありで集計。`【アドネス株式会社】共通除外マスタ` の `除外リスト / 無条件除外ルール` を参照し、追加日以降の新規集客データだけ除外する |
| 日別メール登録件数（UU） | `日付 / メール登録件数（UU） / 累計メール登録件数（UU）`。`2025/01/01 - 2026/03/06` は `Master/output/email_collection_metrics_history_snapshot.json` の固定値を継承し、`2026/03/07` 以降は `【アドネス株式会社】集客データ_メールアドレス（収集）` で最初に確認された日にだけ 1件と数える。共通除外マスタの条件も同時に適用する |
| 複数アドレスユーザー | `顧客ID / メールアドレス数 / メールアドレス`。`【アドネス株式会社】顧客データ_複数イベント（統合） / 顧客マスタ` を参照して、1人で複数メールを持つ顧客を確認する |
| メール集計サマリー | `更新日時 / メール登録件数 / メール登録件数（UU） / メール登録ユーザー数 / 集計開始日 / 最新集計日 / 複数アドレスユーザー数` |
| データソース管理 | 現行の正本・補助参照・出力先だけを載せる。移行用の一時ソースは出さない |
| データ追加ルール | 現行の正本、件数定義、手編集防止、異常検知、更新頻度だけを載せる |

```bash
# 6タブまとめて再生成
python3 System/email_collection_metrics_sheet_sync.py

# 旧2シートから固定スナップショットを再作成
python3 System/email_collection_metrics_sheet_sync.py --refresh-history-snapshot
```

- 定期更新: Orchestrator が `38 */2 * * *` で2時間ごとに再生成
- 異常検知: `開始日ずれ / 参照元タブ0件 / 当日側の件数欠損 / 件数急減` を検知した場合は更新を止める
- 保護: 6タブとも自動生成タブとして保護対象
- 除外: `【アドネス株式会社】共通除外マスタ` の `除外リスト / 無条件除外ルール` を参照し、`追加日` 以降に発生した集客データだけ除外する

---

### 【アドネス株式会社】個別面談データ

個別予約の第1版加工シート。現時点では `個別予約数` だけを正本候補として接続し、`個別予約数（UU）` は将来用の枠だけを持つ。

| タブ名 | 内容 |
|---|---|
| 日別個別予約数 | `日付 / 個別予約数 / 累計個別予約数`。`【アドネス株式会社】個別面談データ（収集） / 個別予約通知ログ` を日別集計し、同一人物の通知が同じ日に10分以内で連続した場合は 1件にまとめる。`【アドネス株式会社】共通除外マスタ` の `除外リスト / 無条件除外ルール` を参照し、追加日以降の新規個別予約だけ除外する |
| 日別個別予約数（UU） | 将来接続用。第1版では空欄の枠だけを持つ |
| 個別予約サマリー | `更新日時 / 個別予約数 / 個別予約数（UU） / キャンセル数 / 除外件数 / 集計開始日 / 最新集計日` |
| データソース管理 | 個別予約数の参照元、最終同期日、更新数、ステータス、メモを持つ |
| データ追加ルール | 第1版の正本、未接続項目、収集データとの役割分担、異常検知、手編集禁止を固定する |

### 【アドネス株式会社】個別面談データ（収集）

個別予約の収集データシート。`Slack #個別予約通知` を継続取得の正本にし、加工前の予約イベントをそのまま保持する。

| タブ名 | 内容 |
|---|---|
| 個別予約通知ログ | `予約イベント日時 / LINE名 / LSTEPメンバーID / Lステップアカウント名 / Lステップリンク / メールアドレス / 電話番号`。`個別予約データ / シート1` の過去データと `Slack #個別予約通知` の bot 通知を 1イベント1行で統合する。手元の友だち一覧CSVは使わず、`Lステップリンク` から会員詳細を live 取得して `メールアドレス / 電話番号` を補完する。`データソース管理` では `Slack通知の取得` と `Lステップ live 補完` を分けて監視する |
| データソース管理 | `Slack #個別予約通知` の継続取得と `Lステップ live 補完` を別々に持ち、最終同期日、更新数、`正常 / 未同期 / 停止` のステータス、メモを管理する。Slack取得では `解析失敗率 / 最新対象通知 / 最新保存通知` まで見える |
| データ追加ルール | 収集データの役割、継続取得の正本、初回移行、重複保存防止、手編集禁止を固定する |

```bash
# 個別面談データの加工タブを再生成
python3 System/booking_metrics_sheet_sync.py

# 個別予約通知ログを更新
python3 System/booking_notification_log_sync.py

# Lステップの個別予約通知を監査
python3 System/scripts/lstep_booking_audit.py

# 書き込みなしで集計確認
python3 System/booking_metrics_sheet_sync.py --dry-run
```

- 定期更新: Orchestrator が `48 */2 * * *` で2時間ごとに再生成
- 通知ログ更新: Orchestrator が `43 */2 * * *` で `Slack #個別予約通知` を読み込み、`個別予約通知ログ` を更新する。通知が取れても解析に失敗した場合は `データソース管理` で `未同期 / 停止` が見えるようにし、`解析失敗率 / 最新対象通知 / 最新保存通知` も確認できる
- live 補完: `booking_notification_log_sync.py` が直近の空欄行に対して `lstep_member_contact_live.py` を呼び、Lステップ会員詳細から `メールアドレス / 電話番号` の補完を試みる。認証が切れている時は `未同期` として記録し、次回へ回す
- live 監査: `lstep_booking_audit.py` は Lステップの認証セッションが生きている時だけ実行できる
- 正本候補: `【アドネス】顧客管理シート / 個別予約集計botログ`
- 継続体制: `Slack #個別予約通知` から `個別予約通知ログ` を作り、将来は `salon_reservation_reserved / reservation.reserved` の予約イベント通知を本命計上元へ段階移行する
- 除外: `【アドネス株式会社】共通除外マスタ` の `除外リスト / 無条件除外ルール` を参照し、`追加日` 以降に発生した個別予約だけ除外する
- 保留: `個別予約数（UU）` `個別実施数`
- 検証用: `★【個別予約完了】★` は日別件数の正本ではなく、Lステップ側の整合確認と冗長系の確認に使う

---

### 【スキルプラス】個別予約AIテレアポ管理

スキルプラス販売導線の中で、AIテレアポ施策を運用するための管理シート。

- 配置: `マイドライブ > 03. スキルプラス > 販売導線`
- 役割: 顧客マスタを読み取り専用の正本にし、`架電一覧` を毎朝自動更新する
- 必須キー: `顧客ID`。`顧客ID` が空欄の行は状態引き継ぎできないため同期対象にしない
- 氏名: `架電一覧` は `D列=氏名` `E列=氏名カナ` `F列=運用ステータス` を基本にする。`氏名` は原則 `姓 + 名`、同じ値が両方に入っているときは1つに直す。`氏名カナ` は `フリガナ（姓） + フリガナ（名）` を使う
- 読み上げガード: `LINE名` は安全に読める日本語表記のときだけ補完する。メールアドレス、絵文字、記号だらけの名前は補完しない。氏名は参照用に持ち、AI の電話冒頭では名前を呼ばず `ご本人様のお電話でお間違いないでしょうか` で入る
- 対象外条件: `デザジュク` / `デザ塾` を `初回流入経路` または `初回購入商品` / `最新購入商品` / `購入商品` に含む人は、スキルプラス販売対象外として `架電一覧` に載せない
- 対象外商品: `デザイン限界突破3日間合宿` / `コンドウハルキ秘密の部屋` を `初回購入商品` / `最新購入商品` / `購入商品` に含む人も `架電一覧` に載せない
- タブ: `架電一覧` / `架電履歴` / `架電成績` / `運用チェック` / `抽出条件・優先順位`
- 自動同期: Mac Mini Orchestrator が `06:15` に `python3 System/teleapo_sync.py sync` を実行
- 並び順: `運用ステータス` を先に見て、`未着手 -> 予約完了 -> 対応完了 -> 対象外` の順に先頭へ出す。その中で `優先順位帯` を並べる。`優先順位帯` は `P1_0-60日` のように base priority と時間帯を合わせて表示し、同一優先帯の中では `初回流入日` を `0-60日 -> 61-180日 -> 181-365日 -> 366日以上 -> 日付不明` の順に並べる。`作成日` は取込日に寄るため使わない
- プルダウン: `架電一覧` の `運用ステータス` / `最新架電結果` と、`架電履歴` の `架電結果` に固定選択肢を入れる。`架電結果` は `応答あり / 留守電 / 応答なし / 拒否 / 番号不備 / 対象外`
- 誤操作防止: `架電一覧` の同期列と、`架電成績` / `運用チェック` / `抽出条件・優先順位` は warning-only 保護を入れる。運用では `架電一覧` の `運用ステータス` / `最新架電結果` を主に触る。不要な古いプルダウンは全列から消してから、必要な列だけ再設定する
- AI更新ルール: AI は `10:00-20:00` の間に `未着手` を優先して架電し、同一顧客への架電は最大3回を前提にする。通話後は `架電一覧` の `運用ステータス` / `最新架電結果` を更新し、`架電履歴` に 1 通話 1 行で追記する。`最新架電結果` と `架電結果` は電話の事実だけを記録し、予約は `個別予約日` の入力で判定する
- 予約扱い: もともと `架電一覧` にいる人に予約シグナルが入ったら `予約完了` に更新して一覧へ残す。新規の予約済み顧客を一覧へ追加はしない。`予約完了` を過去ステータスだけで維持しない
- 架電成績: `架電一覧` と `架電履歴` から、更新日時 / 架電対象数 / 対応完了 / 応答あり / 留守電 / 応答なし / 拒否 / 番号不備 / 予約完了 / 予約完了（架電あり） を自動集計する
- 運用チェック: 最終同期成功日時 / 同期鮮度 / 前回同期件数 / 今回同期件数 / 予約完了だが個別予約日なし / 番号不備だが対象外でない / 架電履歴の顧客ID空欄 / 架電履歴の架電結果空欄 を自動表示する
- 要確認事項: `抽出条件・優先順位` に、森本さんと決める `送信タイミング / 送信システム / 送信文面` を残す。`送信チャネル = メール + LINE`、`動線成功 = 個別予約日が入った時` は確定
- ガード: Google API の一時エラーはリトライし、対象件数が前回比で急減したときは一覧を壊さないために更新を止める

```bash
# シート確認
python3 System/sheets_manager.py info "12RGMUfU8Wj0CCdcRfY7kI56kdATV7wDXjvYmGdQb_Nk"

# 架電一覧を顧客マスタから同期
python3 System/teleapo_sync.py sync

# 書き込みなしで件数確認
python3 System/teleapo_sync.py sync --dry-run
```

---

### 言葉の定義

甲原さんが使う言葉の定義を育てる入力口。人は Google Sheets に追記し、AI はローカル正本を参照する。

- シート入力口: `言葉の定義`
- ローカル正本: `Master/前提/用語定義.md`
- 機械可読版: `Master/前提/用語定義.json`

```bash
# シート同期 + 前提レイヤーの glossary 再生成
python3 System/sheets_sync.py --id "1nkmeWcHmzxPJH1d5veXwTpDoZrsPFH4_3txevqj225Y"
```

---

## 追加予定

- クリエイティブ数値管理
- その他管理シート
