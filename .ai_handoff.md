# 引き継ぎメモ

最終更新: 2026-03-17

## 目的

- `導線ツール` を厳しい 10 点基準まで引き上げる
- 10 点の定義は `役割 / ゴール / 必要変数 / ワークフロー / NG / 正誤判断` を持ち、exact な UI 手順で迷わず実装できる状態

## 最新の厳しめ採点

- Lステップ: `9.8 / 10`
- UTAGE: `9.95 / 10`
- Mailchimp: `9.4 / 10`
- short.io: `9.6 / 10`
- Zapier: `9.5 / 10`

## live 開始前チェック

- まず `python3 System/scripts/funnel_tool_live_readiness.py` を実行する
- `cdp.alive=false` なら、先に `python3 System/scripts/chrome_cdp_bootstrap.py` を実行して 9224 を起こす
- `cdp.alive=false` なら、UTAGE と Zapier の browser exact 化には入らない
- `lstep.auth_alive=true` でも十分ではない。`lstep.account_name` が intended な公式LINE account かを必ず確認する
- 2026-03-16 の current では、`auth_alive=true` でも `account_name=koa800sea` を返した。つまり `認証は生きているが対象 account 文脈はズレている` ケースがある
- `mailchimp.api_alive=true` なら、Journey / Campaign / report の API 読解は進められる
- `shortio.api_alive=true` なら、short.io の作成 / 編集 / 統計 / 台帳同期は進められる
- 2026-03-17 の current では、`sp013` で `作成 -> URL管理シート記録 -> 実 click -> link-stats` まで end-to-end を確認できた
- `link-stats` では `humanClicks=1`、`2026-03-16` に 1 click が入っていることを確認済み
- `Playwright.connect_over_cdp` が timeout する回は、`chrome_raw_cdp.py` の raw fallback を先に試す
- 2026-03-17 の current では、UTAGE と Zapier は raw fallback で `ALREADY_LOGGED_IN_RAW` まで確認できた
- `python3 System/scripts/zapier_family_snapshot.py --limit 5` も raw fallback で再取得できる
  - current では `Webhook -> Mailchimp Add/Update Subscriber` family と `Google Sheets -> webhook post` family を再確認できた
  - `steps=[]` で `signature=""` の `Untitled Zap` row は draft 候補として family の意味読みから外す
- current の Zapier assets には既存の `Untitled Zap` draft が複数残っている
- exploratory relay の cleanup を `Untitled Zap` 一括削除に寄せるのは危険
- 新規 relay の live probe では、作成直後に unique 名へ変えるか、`last modified` と `href` で対象 draft を特定してから削除する
- current の `zapier_cleanup_untitled.py` は `--edit-path` と `--name` 指定を受けられる
- current の `zapier_create_delete_probe.py` は `Assets > Zaps > 甲原 > Create Zap` を主入口に使い、`before/after` の diff で新規 draft の `edit_path` を特定してその row だけ cleanup する
- exact には、folder page を開いてから `Create Zap` を押して builder に入る
- 2026-03-17 の current では `Untitled Zap / Draft` の rename も live で再確認した
  - 左上 title button は exact text 一致ではなく、`Untitled Zap` を含む button として拾う必要がある
  - rename 後は assets 一覧の row 名へ即反映される
  - 一方で `Zap details > Folder = 甲原` は current の row `Location` に即反映されなかった
  - したがって `Folder = 甲原` は Addness の運用ルールとして維持しつつ、current UI では exact-confirmed ではない
- 2026-03-17 の current では `甲原` フォルダ直下 `Create Zap` からの新規 draft 作成も live で再確認した
  - folder URL: `https://zapier.com/app/assets/zaps/folders/019cdd75-b612-ec46-7e5b-bd8a9015a667`
  - `Create Zap` -> `Webhooks by Zapier` -> `Catch Hook` まで進めると assets 一覧に新規 draft が persisted する
  - assets 一覧 row の `Location = 甲原` を確認できた
  - つまり current では `Zap details > Folder` の即時反映は不安定でも、`甲原` フォルダ画面から作れば `Location = 甲原` を exact に満たせる
  - 2026-03-17 以降の正本は、Zapier 新規 relay の主入口を `Assets > Zaps > 甲原 > Create Zap` に固定する
  - `2つ目の action` を足す current 入口は visible text の `Add a step` ではなく、`aria-label=\"Add step\"` の button として扱う
- UTAGE は raw fallback で
  - `商品管理`
  - `アクション設定`
  - `登録経路`
  の `create -> delete` まで再確認済み
- 2026-03-17 の current では `登録経路 > 追加` も live で `create -> delete` を再確認した
- 2026-03-17 の current では `ページ一覧 > 編集 > ページ設定 > 基本情報 > #save-basic` で `管理名称 1変更 -> rollback -> funnel delete` を live で再確認した
- 2026-03-17 の current では `コース一覧 > コース基本情報編集 > 管理名称 > 保存` で `1変更 -> rollback -> row delete` を live で再確認した
  - `管理名称` だけでは保存できない
  - 少なくとも `ファネルステップ` の選択が必要
  - temporary funnel cleanup まで完了
- 2026-03-17 の current では `商品詳細管理 > 追加` で `実行するアクション / 開放するバンドルコース` の chain save も live で再確認した
  - probe: `python3 System/scripts/utage_detail_chain_probe.py`
  - exploratory product 配下で detail を 1件追加
  - `実行するアクション = 【スタンダード】事業構築コース　講義保管庫解放アクション`
  - `開放するバンドルコース = スキルプラス講義保管庫全開放`
  - save 後に detail row が `1件` 出ることを確認
  - `detail edit` を再度開いて selected value を再読込
    - `action_id = 1127`
    - `bundle_id = 8403`
  - product row delete で cleanup 済み
- 2026-03-17 の current では `ページ > 追加` も raw probe で `create -> delete` を再確認した
  - `名称` を正しく入れないと `名称は必ず指定してください。` で止まる
  - `ページ一覧` の actual URL は created row action から取る
  - `row_link` の page id と `編集` route の page id が一致しないことがある
- 2026-03-17 の current では `バンドルコース一覧 > 追加` も raw probe で `create -> delete` を再確認した
  - 必須は `バンドルコース名` と `追加するコース` の最小選択
  - `before_count = 0` `after_create_count = 1` `after_delete_count = 0`
- 2026-03-17 の current では `コース一覧 > 追加` も raw probe で `create -> delete` を再確認した
  - 必須は `コース名` `管理名称` `リンク先URL` の最小入力
  - `before_count = 0` `after_create_count = 1` `after_delete_count = 0`
- 2026-03-17 の current では `レッスン一覧 > 追加` も raw probe で `create -> delete` を再確認した
  - 必須は `グループ` `レッスン名` `コンテンツ` の最小入力
  - save 後に一覧へ `1件` 出ることを確認し、その後 row delete で `0件` に戻るところまで確認済み
- 2026-03-17 の current では `レッスン一覧 > 編集` も live で再確認した
  - `レッスン名` を 1変更
  - 値の変更確認
  - 元の値に rollback
  - row delete まで完了
  - `コンテンツ` は hidden textarea なので、Playwright の `fill()` ではなく JS で値を入れる必要がある
- Mailchimp は raw fallback で `login/tfa-post` まで確認できた
- helper は hanging せず `LOGIN_NEEDS_TFA` で返る
- browser 上では `Success! Your SMS verification code is on its way! Check your phone to catch it!` まで確認した
- それでも helper は `found=false` だった
- current blocker は `SMS送信` ではなく `today の LINE group-log 取得`
- その後の再確認では `api/group-log?date=2026-03-17` は `groups=4` を返した
- つまり current blocker は `today の group-log 全体が空` ではなく、
  - today の group-log 自体は取れている
  - ただし `Mailchimp認証` の `group_id = Ce2900a5b8c1efb939b3778262f1a9808` が today payload に現れていない
  こと
- 2026-03-17 の current payload では、today の `groups` は 4 件とも `group_name=""`
  - `C2978001e19bfd7ea9608a586f372173c`
  - `C330ef524daef13701ef3bd1f8127207f`
  - `C5d39c4fe007af6773f770eb991b77cac`
  - `Ce2cd4420d4b7582a89c00a7294256f95`
- 本文検索では `Mailchimp` / `認証コード` / `6桁コード` に一致する today message は取れなかった
- つまり current blocker は helper の優先順ではなく、`today の group-log そのものに Mailchimp 認証 message が載っていない` こと
- `Resend code` 直後に `mailchimp_tfa_code_helper.py --wait` を流した current probe では `found=false`
- つまり `group-log API` では today の認証コードが取れていない
- `mailchimp_tfa_code_helper.py` は current で
  - `Mailchimp認証` グループ優先
  - known `group_id = Ce2900a5b8c1efb939b3778262f1a9808` も優先
  - 複数の code pattern 許容
  - warning suppress
  に更新済み
  - `group_id -> group_name` の逆引き fallback も追加済み
  - `python3 System/scripts/mailchimp_tfa_code_helper.py --max-days 7 --max-age-minutes 10080`
    で `2026-03-16T23:46:49 / code=506232 / group_id=Ce2900a5b8c1efb939b3778262f1a9808 / group_name=MailChimp認証`
    を再取得できた
  - current の `group-log API` では、この group が `group_name=""` で返る回がある
  - 2026-03-16 の履歴ログでは、`group_name=""` でも `group_id = Ce2900a5b8c1efb939b3778262f1a9808` 優先で code を再取得できた
  - つまり helper 自体は `group_name 空文字` に耐えられる
  - それでも `found=false` の時は、helper ではなく server 側 group-log 取得経路を疑う
- `mailchimp_login_helper.py` の Playwright 側 TFA は current で `wait_for_mailchimp_code(...)` を使う
- したがって current の残差は helper の参照漏れではなく、本当に `today の LINE group-log 取得` 側
- 2026-03-17 以降の運用 fallback は、当日の code を helper で取れない時は `Mailchimp認証` グループを人が確認して入力する
- Mailchimp API では
  - `saved segment create -> delete`
  - `ensure-member -> add-tag -> remove-tag -> delete-permanent`
  まで live exact 済み
- `ensure-member` 直後の `add-tag` は一時的に `404` になる回があった
  - current では `member` で見えてから再試行すると成功した
- `members/{hash}` への `DELETE` は current で `405`
  - cleanup は `POST /actions/delete-permanent`

## readiness snapshot

- CDP: `true`
- Lステップ auth: `false`
- Lステップ account: `null`
- Lステップ next_action:
  - `cookie source が全滅で、login page は reCAPTCHA enabled=true。 current session の再取得には browser login が必要。`
- Mailchimp API: `true`
- short.io API: `true`
- UTAGE live browser: `true`
- Zapier live browser: `true`
- Mailchimp browser:
  - `2-factor authentication | Mailchimp`
  - `https://us5.admin.mailchimp.com/login/tfa-post`
  - `LOGIN_NEEDS_TFA`

## 実際に触って確認できている範囲

- Lステップ
  - `フレックスメッセージ` の `作成 -> テスト送信 -> 削除`
  - `自動応答` の `作成 -> 一覧確認 -> 削除`
  - `回答フォーム` の `作成 -> テスト送信 -> 削除`
  - `アクション管理`
    - `新しいアクション` は side drawer
    - `タグ操作` は文字入力だけでは保存できず、`タグ選択` が必須
    - `この条件で決定する` で validation error
      - `登録に失敗しました`
      - `1.[タグ編集]タグは必須です。`
    - unsaved drawer close では
      - `保存されていない変更があります。本当に閉じますか？`
      - `キャンセル / 閉じる`
      の confirm が出る
  - 流入経路命名
    - SNS は `（アカウント名）集客媒体 _ファネル名_イベント名`
    - `直リストイン = 直接LINE登録`
    - `リストイン = メール登録後にLINE登録`
- UTAGE
  - representative な `ページ / 商品管理 / 商品詳細管理 / 購入後アクション / バンドルコース / 会員サイト / 動画管理 / メディア管理` の読解
  - runtime から code 領域の値取得
  - `ファネル -> 追加 -> 空白のファネル -> 詳細 -> このファネルを追加する` の current 導線確認
  - `System/scripts/utage_funnel_create_delete_probe.py`
    - `ファネル -> 追加 -> 空白のファネル -> 詳細 -> このファネルを追加する -> 一覧確認 -> row delete`
    - current では `button.btn-add` の `form.submit()` が最も安定
    - row action は `slug route`、`削除` だけ `numeric id form`
    - probe 実績
      - `before_count = 257`
      - `after_create_count = 258`
      - `created_id = 232380`
      - `after_delete_count = 257`
      - `deleted = true`
  - `商品管理` の exploratory product `create -> rollback`
    - `ZZ_TEST_20260316_UTAGE_product_exact`
    - row action: `開く / 編集 / コピー / アーカイブ(非表示化) / 削除`
  - `開く = 商品詳細管理`
    - route: `/product/{product_id}/detail`
  - `商品詳細管理 > 追加`
    - route: `/product/{product_id}/detail/create`
    - exact な主要ラベル:
      - `名称`
      - `支払方法`
      - `決済代行会社`
      - `決済連携設定`
      - `支払回数`
      - `金額`
      - `登録するシナリオ`
      - `実行するアクション`
      - `開放するバンドルコース`
    - exact な主要 option:
      - `支払方法`
        - `クレジットカード払い`
        - `銀行振込`
      - `決済代行会社`
        - `Stripe`
        - `UnivaPay`
        - `AQUAGATES`
        - `テレコムクレジット`
        - `FirstPayment`
      - `支払回数`
        - `一回払い`
        - `複数回払い・分割払い`
        - `継続課金`
      - `登録するシナリオ`
        - current の `ユーザー登録` 系 scenario が大量に並ぶ
      - `実行するアクション`
        - current の `バックエンド` 系
        - `講義保管庫解放アクション` 系が並ぶ
      - `開放するバンドルコース`
        - `スキルプラス講義保管庫全開放`
        - `PRM JV用`
        - `プライム会員限定`
        などが並ぶ
    - exploratory detail
      - `ZZ_TEST_20260316_223638_UTAGE_detail_create_probe_detail`
      - 最小値で `保存成功`
      - detail 一覧に `1件追加`
      - cleanup は `UTAGE_detail_create_probe` pattern で `0件`
  - 2026-03-17 live probe の再確認
    - product create / delete: `before 0 -> after_create 1 -> after_delete 0`
    - detail create / save / cleanup: `保存成功`, detail 一覧 `1件追加`, cleanup 後 `0件`
    - detail create / save / cleanup を raw fallback でも再確認
      - `before_product_count = 0`
      - `after_product_count = 1`
      - `detail_row_count = 1`
      - `save_result.error_texts = []`
      - save 後 URL は `.../detail` 一覧
    - action create / save / delete: `保存成功`, action 一覧 `1件追加`, cleanup 済み
    - funnel create / delete: `before 258 -> after_create 259 -> after_delete 258`, `created_id = 232420`
    - page create / delete:
      - temporary funnel を `create`
      - row action の `ページ一覧` から actual slug を取得
      - `追加 -> 名称 -> 保存`
      - page row 出現を確認
      - temporary funnel `delete` で rollback
      - current 実績
        - `page_list_url = /funnel/4eAriUd6li3Z/page`
        - `create_url = /funnel/4eAriUd6li3Z/create`
        - `保存後 current_url = /funnel/4eAriUd6li3Z/page/ZKxXY8aywnwm`
        - `row_link = /page/ZKxXY8aywnwm#list-ZKxXY8aywnwm`
        - `edit_link = /page/0opi5kesOlJq/edit`
        - `deleted = true`
      - 学習
        - create template の slug と created funnel の slug は一致しない
        - actual な `ページ一覧` URL は created row action から取る
        - current では `row_link` の page id と `編集` route の page id が一致しないことがある
- Mailchimp
  - regular `Campaign` の draft 作成 / 削除
  - `Journey` の `Build from scratch -> Name flow -> Audience -> trigger`
  - `mailchimp_journey_snapshot.py --list-current --count 20` で current matrix 再取得
  - `mailchimp_campaign_snapshot.py --limit 10` で recent regular campaign を再取得
    - `main CTA = direct LINE`: `10 / 10`
    - `hyperlink_mismatch_count = 0`: `10 / 10`
  - `saved segment 1件` の create / delete
    - `zz_test_segment_20260316_exact`
    - `id=4641613`
    - delete `204`
  - safe exploratory member で `tag 1件` の `add -> rollback`
    - `koa800sea.nifs+1006@gmail.com`
    - `zz_test_tag_20260316_exact`
    - `tags_count 0 -> 1 -> 0`
  - `tag-search` の一覧読解
  - `search-members` による safe exploratory member 探索
  - `report` の漏斗確認
- short.io
  - テスト短縮URLの `作成 -> resolve -> 編集 -> resolve -> 削除`
  - API 確認 / URL管理シート同期
- Zapier
  - representative relay の editor / step 読解
  - `Create Zap` を開くと `New Zap | Zapier` の editor へ入る
  - `Create Zap` を開いただけでは assets 一覧に `Untitled Zap` は persisted しない current 挙動
  - `Trigger > Webhooks by Zapier > Catch Hook` まで選ぶと assets 一覧に `Untitled Zap` が persisted する
  - `Action > Mailchimp > Add/Update Subscriber` まで選択し、`Test` stage 到達まで exploratory 済み
  - exploratory `Untitled Zap` は `assets 一覧 row action Delete -> Delete` で `0件` まで cleanup 済み
  - `python3 System/scripts/zapier_cleanup_untitled.py` でも `after_count=0` まで cleanup 済み
  - persisted 済み draft は `assets 一覧 row action Delete -> cleanup`
  - `python3 System/scripts/zapier_create_delete_probe.py --with-action`
    - `Create Zap -> Trigger > Webhooks by Zapier > Catch Hook -> Action > Mailchimp > Add/Update Subscriber -> Test -> Delete Zap` を live 完了
  - `python3 System/scripts/zapier_create_delete_probe.py --with-action --action-app webhook-post`
    - `Create Zap -> Trigger > Webhooks by Zapier > Catch Hook -> Action > Webhooks by Zapier > POST -> Test -> Delete Zap` を live 完了
    - つまり external account 不要の 1 action relay も current UI で exact に通る
- `python3 System/scripts/zapier_create_delete_probe.py --with-action --with-second-action`
    - cleanup は成功
    - `trigger_app = Webhooks by Zapier`
    - `trigger_event = Catch Hook`
    - `action_app = Mailchimp`
    - `action_event = Add/Update Subscriber`
    - `second_action_app = Webhooks by Zapier`
    - `second_action_event = POST`
    - `second_action_selected = true`
    - `post_second_action_stage = Test`
- `python3 System/scripts/zapier_second_action_probe.py`
    - first action = `Webhooks by Zapier -> POST`
    - before:
      - URL = `.../draft/.../setup`
      - step node = `1. Catch Hook / 2. POST`
      - body marker = `Action / Test / Publish`
    - second action 入口を開くと URL が `.../setup -> .../fields`
    - after:
      - step node は `1. Catch Hook / 2. POST` のまま
      - `Add a step` は visible marker に出ていない
    - つまり current は `2つ目の action picker` ではなく `既存 action の fields` を再度開く回がある
  - current family snapshot
    - `Webhook -> Mailchimp Add/Update Subscriber`: `21本`
    - `Google Sheets -> webhook post`: `3本`
    - old webhook -> Mailchimp: `1本`

## 追加で exact 化した helper

- Lステップ template 分解
  - `python3 System/scripts/lstep_template_catalog.py list --limit 30`
  - `python3 System/scripts/lstep_template_catalog.py inspect --id <template_id>`
  - `python3 System/scripts/lstep_template_snapshot.py --url "<edit_url>"`
  - 一覧種別、block 構成、action 実体、visible UI を分けて読む
- UTAGE live probe
  - `python3 System/scripts/utage_product_create_delete_probe.py`
  - `商品管理 -> 商品追加 -> 保存 -> row delete` の exploratory create/rollback を current UI で確認
  - `python3 System/scripts/utage_detail_form_snapshot.py`
  - `商品管理 -> 商品詳細管理 > 追加` の form / option を live で取得
  - `python3 System/scripts/utage_detail_create_delete_probe.py`
  - `商品管理 -> 商品詳細管理 > 追加` を最小値で `保存 -> detail一覧確認 -> cleanup`
  - `python3 System/scripts/utage_action_list_snapshot.py`
  - `アクション設定` 一覧の `追加` 導線と representative row を live で取得
  - `python3 System/scripts/utage_action_form_snapshot.py`
  - `アクション設定 > 追加` の form / option を live で取得
  - `python3 System/scripts/utage_action_create_delete_probe.py`
  - `アクション設定 > 追加` を最小値で `保存 -> 一覧確認 -> form-delete cleanup`
  - `python3 System/scripts/utage_cleanup_test_products.py`
  - `UTAGE_detail_probe` 系 exploratory 商品を cleanup
- Zapier live probe
  - `python3 System/scripts/zapier_create_delete_probe.py`
  - `Create Zap -> Trigger -> Webhooks by Zapier -> Catch Hook -> persisted draft -> assets cleanup` を current UI で確認

## まだ live で厚くすべき範囲

- Lステップ
  - `アクション管理 / リッチメニュー` の create -> test -> cleanup
- UTAGE
  - `ページ / 購入後アクション / 会員サイト` の small change -> rollback
  - `登録経路` は temporary funnel 配下で `create -> delete` 済み
- Mailchimp
  - `Journey 1本` の create -> test -> cleanup
  - `tag 1件` の UI 側 create -> rollback
  - `today の認証コード取得経路` の current 化
- Zapier
  - `Create Zap -> trigger 1つ選択 -> Test -> Delete Zap` の live 実作成

## current live blocker

- Mailchimp
  - `python3 System/scripts/mailchimp_journey_create_delete_probe.py` は current で `ensure_login(AUTOMATIONS_URL)` を先に実行する
  - それでも current は `LOGIN_NEEDS_TFA`
  - browser 側の exact 化は、2段階認証を通した session がある時だけ進める
- Zapier
  - `python3 System/scripts/zapier_create_delete_probe.py --with-action` は current で 1 action relay の create/test/delete を完了
  - `zapier_create_delete_probe.py` は raw fallback を持つ
  - 2026-03-17 の current では raw fallback で `step-node` の親要素を押す必要があることまで特定した
  - この修正で `Search apps` input が visible になるところまでは raw で再現できた
  - 一方で persisted draft の判定は timing と assets 一覧文脈の影響を受ける回があり、raw fallback だけでは毎回 `Untitled Zap` row を拾い切れない
  - つまり create probe は `Playwright.connect_over_cdp が生きている回` を優先する
  - `python3 System/scripts/zapier_create_delete_probe.py --with-action --with-second-action` は cleanup まで通る
    - `second_action_app = Webhooks by Zapier`
    - `second_action_event = POST`
    - `post_second_action_stage = Test`
  - `python3 System/scripts/zapier_create_delete_probe.py --with-action --with-second-action --action-app webhook-post --second-action-app mailchimp`
    も cleanup まで通る
    - `second_action_app = Mailchimp`
    - `second_action_event = Add/Update Subscriber`
    - `post_second_action_stage = Test`
  - first action 後の current builder には
    - `Choose an event`
    - `Select`
    - `Add account to continue`
    が visible
- 2026-03-17 の `zapier_second_action_probe.py` では second action 入口を開くと `.../fields` に戻る回を確認した
- current の実 UI では、`Add a step` は visible text ではなく `aria-label=\"Add step\"` の button として出る
- `zapier_create_delete_probe.py` はこの selector を拾うよう更新済み
- つまり second action の主 blocker は解消済み
- 残差は representative family を増やすこと
  - current owner 表示は `【世捨人東大生】 ぜんT (Personal)`
  - `Create Zap` 後の persisted 判定は timing と assets 一覧文脈の影響を受ける

## live 準備 helper

- `python3 System/scripts/chrome_cdp_bootstrap.py`
  - 9224 の Chrome CDP が死んでいる時に、live 作業用の Chrome を起こす
  - `live browser ready=false` の時は、まずこの helper を試してから login helper に進む

## 10点までの最短ルート

- Lステップ
  - `回答フォーム 2問`
  - `アクション管理 1複合`
  - `リッチメニュー 2ボタン`
- UTAGE
  - `商品管理 / 商品詳細管理 / 購入後アクション 1本`
  - `開放するバンドルコース` までの downstream smoke
- Mailchimp
  - `Journey 1本`
  - `tag 1件` の UI 側 create
- Zapier
  - `Catch Hook -> Mailchimp Add/Update Subscriber`
    の最小 relay を `Create Zap -> trigger 1つ選択 -> Test -> Delete Zap`
- short.io
  - 実案件 1 本を `判断 -> 作成 or 差し替え -> URL管理シート更新 -> click / 統計確認` まで通す
  - live exact はかなり埋まっているので、残差は実案件反復

## next

1. Mailchimp
   - `Journey 1本`
   - `tag 1件`
   を live で閉じる
2. Zapier
   - 最小 relay を `Create -> Test -> Delete`
3. UTAGE
   - `1変更 -> downstream 確認 -> rollback`
4. Lステップ
   - `アクション管理 / リッチメニュー` の複雑パターン

## 2026-03-17 実案件: スキルプラス公式TV 概要欄導線

- 目的
  - YouTube `スキルプラス公式TV` 概要欄から `スキルプラスの無料体験会に今すぐ参加する` 導線を作る
- Lステップ account
  - `スキルプラス (@496sircr)`
- 流入経路名
  - `（スキルプラス公式TV）YouTube _スキルプラス_直リストイン`
- 作成済み route
  - `流入経路分析` id `2359214`
  - `follow_url` = `https://liff.line.me/2006618892-P3oWzoBb/landing?follow=%40496sircr&lp=GnsXlA&liff_id=2006618892-P3oWzoBb`
  - `QR` は Lステップ row の `QR` ボタンから取得可
  - `広告挿入タグ` = `<script src="https://ltag.jp/lc/GnsXlA"></script>`
- 設定内容
  - route tag 追加
  - `【新規】（スキルプラス公式TV）YouTube _スキルプラス_直リストイン` 追加
  - `⭐️必ずつけてね！...` の既存 tag 追加
  - `友だち情報 [新規流入]` と `友だち情報 [最終流入]` を route 名で更新
  - `メインリッチメニュー（企画専用LINE遷移）に変更`
  - `無料体験会訴求シナリオ` を開始
- 既知の差分
  - 既存 family にある `友だち情報 [新規流入日] = action_date` 更新は、current API では 422 になったため今回の route には未設定
  - エラー: `6.[友だち情報変更]定数は必須です。`
- short.io
  - folder = `SNS(YT)`
  - path = `sp013`
  - title = `（スキルプラス公式TV）YouTube _スキルプラス_直リストイン`
  - short URL = `https://skill.addness.co.jp/sp013`
  - link id = `lnk_5tf2_nbmiXw6QvDnuOxsMb64qB`
- URL管理シート
  - `01_全体台帳` row `453`
  - `YouTube` row `20`
  - 記録値
    - `ファネル名` = `スキルプラス`
    - `設置場所` = `YouTube概要欄_スキルプラス公式TV`
    - `リンクタイトル` = `（スキルプラス公式TV）YouTube _スキルプラス_直リストイン`
    - `リンクURL` = `https://skill.addness.co.jp/sp013`
    - `遷移先名` = `スキルプラス公式LINE`
    - `遷移先リンク` = 上記 `follow_url`
    - `更新日` = `2026/3/17`
    - `状態` = `正常`
- 対人共有
  - `【アドネス】社外メディア広報`
  - `@しおり` 宛に short URL を共有済み
  - 共有文面
    - `スキルプラス公式TV の概要欄用に、無料体験会参加導線の短縮URLを作成しました。`
    - `https://skill.addness.co.jp/sp013`
- 再発防止
  - 流入経路は `既存 family から選ぶ` のではなく `流入前オファー / 流入時状態 / 開始シナリオ` の順で決める
  - Lステップ は browser 上でログイン済みに見えても API セッションが切れていることがある
  - API で route を作った時は、row 右側の action 説明と保存結果を両方確認する
  - LINE で `メンション` が必要な共有は `@名前` 文字列では足りない。`group_id / user_id / 本物メンション送信` が必要

## AI秘書 LINE 通知の exact 追加

- `System/line_bot/app.py`
  - `/notify/mention`
    - 指定グループに本物メンション付きで送信
  - `/api/group-members`
    - 指定グループの `user_id / user_name` 一覧取得
- `System/mac_mini/agent_orchestrator/notifier.py`
  - `send_line_notify_with_mention(...)`
  - `get_line_group_members(...)`
- 今後、LINE で `@名前` が必要な共有は plain text `/notify` を使わない
- current blocker
  - Render 側の `/notify/mention` と `/api/group-members` は 2026-03-17 時点で `404`
  - つまりコード実装と push は済みだが、本物メンション送信は deploy 反映待ち
