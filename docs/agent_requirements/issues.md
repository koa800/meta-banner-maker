# Issues（未解決事項・要確認論点）

> 要件定義 draft_v1 作成時に検出した不明点・矛盾・判断保留事項

---

## 優先度: 高（実装設計前に確認必須）

### #1 Cursor内基盤とMac Mini常駐システムの関係 — **解決済み**

**決定:** A: **共存**（役割分担）

- Mac Mini: バックグラウンド自動化（秘書+Orchestrator+日向）— 変更なし
- Cursor (MacBook): 対話的な業務代行 — **Claude Code + Codex CLI の併用**
- 共有基盤: `cursor/` ディレクトリのファイル・フォルダが全ツールの共通資産
- 同期: git経由（Mac Miniが5分ごとに git pull）

> 2026-03-07 甲原さん確認済み: 「ツールは手段。基盤（ファイル・フォルダ）を共有して、役割に応じてツールを使い分ける」

---

### #2 「Cursor内で動く」の具体的な意味 — **解決済み**

**決定:** E: **複数CLIツールの併用基盤**（A+Dの発展形）

- Cursor はエディタ + ターミナル環境として使う
- ターミナル内で **Claude Code**（ブラウザ操作・既存システム保守）と **Codex CLI**（コード実装・リサーチ・推論）を役割に応じて使い分ける
- 両ツールとも同じ `cursor/` ディレクトリを読み書きするため、知識は自動共有
- ルールファイルは CLAUDE.md（Claude Code用）と AGENTS.md（Codex CLI用）で分離管理、行動原則は同期

> 2026-03-07 甲原さん確認済み: 「全コンテキストがClaude側にあるというのは共有すればいい話」

---

### #3 CR単位の広告データ接続（最大のブラインドスポット）

**問題:** 「秘書の視界マップ」のA項。CR単位のCPA/CTR/CPM/CVR が見えない。これがないとTier 1のB1+B2（広告日次確認）のAI化が「月別サマリの確認」止まりになる。

**選択肢:**
- A: Meta/TikTok/YouTube広告APIを直接接続（技術的に可能だが新規サービス導入→確認必要）
- B: 広告運用担当（新井さん等）にデータ共有を依頼（秘書の視界マップの推奨方法）
- C: Looker Studioに媒体別のCRデータを追加してもらう

**影響:** Tier 1の「CPA改善」効果が大きく変わる。

---

### #4 日向エージェントの再開方針

**問題:** 日向は「Phase 1稼働中」だが「2026-03-06 一時停止」。停止理由は「スキル整備後に再開」。

**確認事項:**
- どのスキルが整備されれば再開するか
- 新しいCursor基盤と日向の関係（日向をCursor基盤に統合するか、独立して再開するか）
- 日向のLv.1→Lv.2昇格の見通し

---

### #5 コスト配分の方針

**問題:** 秘書v2設計書ではGPT-5.4（月~$58）がデフォルトだが、Cursor基盤でさらにLLM使用が増える。

**現状のコスト:**
- 秘書v2: ~$58/月（GPT-5.4の場合）
- Claude Max: $200/月（日向用）
- Claude Code CLI: サブスク（秘書バッチ用に移行済み）
- 画像生成: ~$20/月（Gemini予算上限¥2,000）

**確認事項:**
- Cursor基盤のLLM使用は追加API課金か、既存Maxプラン内か
- 合計月額の上限

---

## 優先度: 中（実装中に解決可能）

### #6 profiles.jsonの二重管理リスク

**問題:** profiles.jsonは「人物プロファイル（58名）」と「ゴール実行エンジンの統一エージェントスキーマ（人間+AI+ワークフロー）」の2つの役割を持つ設計。現時点では人物プロファイルの方がメインで、AIエージェントの登録は設計書上のみ。

**リスク:** 両方の用途で同時に編集が発生するとマージ衝突が起きる。

**提案:** 人物プロファイルとエージェントスキーマを分離するか、統合するかを明確にする。

---

### #7 Pinecone依存の将来性

**問題:** Q&A自動回答がPinecone（ベクトルDB）に依存。環境変数未設定時はスキップされる設計だが、Cursor基盤でのナレッジ検索にもPineconeを使うか、別の方式にするか。

**選択肢:**
- A: Pinecone継続（Q&Aはそのまま。Cursor基盤でも利用）
- B: ローカルベクトルDB（Chroma等）に移行（中から外の原則に合致）
- C: LLMのコンテキストウィンドウ拡大で不要になる可能性を待つ

---

### #8 秘書v2とゴール実行エンジンの統合度

**問題:** 秘書v2（conversation.py + llm_router.py）とゴール実行エンジン（coordinator.py）が別系統で実装されている。

**現状の使い分け:**
- 秘書v2: LINE会話（秘書グループ・メンション・Q&A）
- ゴール実行エンジン: 複雑なゴール（「来週の商談に備えて」等）

**Cursor基盤では:** 両方の機能が必要。統合するか、使い分けを維持するか。

---

## 優先度: 低（運用後に対応可能）

### #9 learning/ ディレクトリの肥大化

**問題:** action_log.json（50件上限）、feedback_log.json（50件上限）は管理されているが、insights.md やhinata_memory.md に上限がない。

---

### #10 Orchestratorのタスク数増加

**問題:** 既に15+タスクが稼働中。Cursor基盤からさらにスケジュールタスクが増える可能性。config.yamlの管理が複雑化するリスク。

---

### #11 テスト基盤の不在

**問題:** 既存システムにユニットテスト・統合テストの基盤がない。エージェント基盤の品質保証方法が未定義。

---

## 矛盾・不整合

### #12 project_structure.md と AI秘書作成.md の重複

`Master/knowledge/project_structure.md` の「ゴール実行エンジン」セクションと `Project/4_AI基盤/ゴール実行エンジン設計.md` に重複記述あり。project_structure.md 側が古い可能性。

**提案:** project_structure.md のゴール実行エンジン記述を削除し、ゴール実行エンジン設計.md をSingle Source of Truthにする。

---

### #13 日向のinterface設計と実態の乖離

ゴール実行エンジン設計書では日向が `type: "human"` + `interface.method: "line_message"` で登録されているが、実際の日向はAIエージェント（`type: "ai"`）でSlack経由。設計書の例示が古い。

---

## 危険ポイント（実装前に注意）

### #14 認証情報の散在

OAuth トークンが `System/credentials/`、`System/token*.json`、Mac Mini `~/agents/token*.json` に散在。新しい基盤でこれらをどう参照するか統一が必要。

### #15 破壊的操作の防止

既存システムでは `bypassPermissions` + 明示的禁止リストで制御。Cursor基盤でも同等の安全ガードが必要。特に:
- CDP同期（57,750人のデータ）への書き込み
- LINE/Chatworkへの外部送信
- git force-push
